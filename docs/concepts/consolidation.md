# Consolidation

HyperMind consolidation is a budgeted ledger run, not an in-place rewrite. NREM groups frozen observations and may attach, revise, or mint a memory only after citation, independence, quality, and rewrite checks pass. REM connects memories, synthesizes novel abstractions, audits existing memories, and schedules review. Provider output is untrusted until these deterministic checks accept it.

Every run declares its scope, cadence key, generation, prompt versions, and limits for calls, tokens, cost, and wall time. Phase progress is persisted with deterministic attempt prefixes so a restart resumes without duplicating accepted work. Derived records retain `derived_inference` authority and cite byte ranges from the run's frozen candidates. Minting requires three independent source roots spanning at least two conversations.

Before any of those checks run, a merge response is measured against a versioned extraction contract: the seven fields the provider was asked for must all be present, none may be invented, and every identifier the model returns is bounded in characters rather than bytes, so a label written in any script is held to the same limit and can never be long enough for the ledger to reject it later. A response that breaks the contract is a counted drop naming the offending field, never a staged record, and the reported detail carries the field name and the observed length rather than the value itself.

NREM candidates are extracted under a per-run concurrency ceiling rather than one strictly at a time: the retained clusters are dispatched to the provider in parallel up to a ceiling the deployment sets (`HM_CONSOLIDATION_PARALLEL`, four by default, clamped to one through sixty-four), and their results are put back into source order before anything is staged, so the minted records still appear in the order the clusters were produced. Every candidate ends the pass with exactly one recorded outcome: extracted when the call ran and produced a decision, failed when the call ran and the provider returned an error, skipped when the candidate was never dispatched. The first failure stops new dispatch, but calls already in flight are drained to completion and their usage is still counted, so no answer that was already paid for is thrown away. Skipped candidates are folded into the run's dropped candidate count, and the four counts are reported per run so an operator can tell what succeeded, what the provider refused, and what was never attempted.

A run does not wait for the provider to answer before it knows whether it can afford the attempt. Before each provider call the run reserves the two dimensions that are knowable in advance — one call, and the request's output ceiling as an upper bound on the tokens it can return — and the attempt proceeds only when the already settled usage, the reservations still outstanding, and the new request all fit inside the declared limits together. The real usage is settled afterwards, including the usage of an attempt that was refused or truncated, so a failed call is charged rather than forgiven, and a reservation that never reached the provider is released instead. A retry is admitted the same way and at its raised ceiling, so no attempt in a run, retry included, can be made outside the budget the run declared. Wall time stays with the run's elapsed-time check rather than the reservation.

When a provider answer is classified as a fault rather than a result, the run may repair it exactly once. A refusal is never re-asked, because putting the same question again does not repair a decision to decline. A truncated answer is re-asked at the raised output ceiling the run declared for retries, and only when that raised ceiling is genuinely higher than the one that already truncated; a malformed or incomplete answer is re-asked unchanged, so its second request is the same bytes as its first. Every re-ask reserves the call and the ceiling it will actually use before it is sent, and when the reservation refuses it the cluster is dropped and counted as reservation-exhausted instead of the run overrunning. There is no second retry rung, and the number of repairs a run spent is reported alongside its call count.

Publishing is atomic. Derived events remain staged and invisible until all relevant projections have applied them and `ConsolidationClosed` successfully compare-and-swaps the active generation. `ConsolidationRetracted` restores the parent generation immediately; cleanup is separate and cannot delay rollback.

After a run publishes, the relationships of the active generation are turned into relationship representations and embedded in the background, in their own embedding space rather than the document one. Each relationship embedding carries the LSN of the asserting `EdgeAsserted` event as its target and is built from the source LSNs that produced the relationship, so a relationship can be ranked and explained without re-reading the run. The build is bounded, runs only after the publish append succeeds, and degrades to a gap and a warning instead of failing the run; the recall path never embeds relationships.

The slice-6 evaluation gate measures zero lossy rewrites on the cortex incident fixtures, rejection of invalid citations and insufficient roots, crash recovery before and after publication, lost-ack deduplication, rollback of leased views, and consolidation-enabled LongMemEval when its author dataset is installed.
