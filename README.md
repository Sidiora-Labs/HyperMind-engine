# HyperMind

HyperMind is a durable, evidence-aware memory engine for agents. It is being built as eight independently runnable vertical slices under the contract in [`spec/hypermind-01/spec.kvx`](spec/hypermind-01/spec.kvx).

Build and test the current workspace with:

```sh
cargo build --workspace
cargo test --workspace
```

