// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class Hello {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Hello {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsHello(bb:flatbuffers.ByteBuffer, obj?:Hello):Hello {
  return (obj || new Hello()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsHello(bb:flatbuffers.ByteBuffer, obj?:Hello):Hello {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Hello()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

protoVersion():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

connectionId(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

connectionIdLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

connectionIdArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

capabilityToken(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

capabilityTokenLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

capabilityTokenArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startHello(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addProtoVersion(builder:flatbuffers.Builder, protoVersion:number) {
  builder.addFieldInt16(0, protoVersion, 0);
}

static addConnectionId(builder:flatbuffers.Builder, connectionIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, connectionIdOffset, 0);
}

static createConnectionIdVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startConnectionIdVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addCapabilityToken(builder:flatbuffers.Builder, capabilityTokenOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, capabilityTokenOffset, 0);
}

static createCapabilityTokenVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startCapabilityTokenVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endHello(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 6) // connection_id
  builder.requiredField(offset, 8) // capability_token
  return offset;
}

static createHello(builder:flatbuffers.Builder, protoVersion:number, connectionIdOffset:flatbuffers.Offset, capabilityTokenOffset:flatbuffers.Offset):flatbuffers.Offset {
  Hello.startHello(builder);
  Hello.addProtoVersion(builder, protoVersion);
  Hello.addConnectionId(builder, connectionIdOffset);
  Hello.addCapabilityToken(builder, capabilityTokenOffset);
  return Hello.endHello(builder);
}

unpack(): HelloT {
  return new HelloT(
    this.protoVersion(),
    this.bb!.createScalarList(this.connectionId.bind(this), this.connectionIdLength()),
    this.bb!.createScalarList(this.capabilityToken.bind(this), this.capabilityTokenLength())
  );
}


unpackTo(_o: HelloT): void {
  _o.protoVersion = this.protoVersion();
  _o.connectionId = this.bb!.createScalarList(this.connectionId.bind(this), this.connectionIdLength());
  _o.capabilityToken = this.bb!.createScalarList(this.capabilityToken.bind(this), this.capabilityTokenLength());
}
}

export class HelloT {
constructor(
  public protoVersion: number = 0,
  public connectionId: (number)[] = [],
  public capabilityToken: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const connectionId = Hello.createConnectionIdVector(builder, this.connectionId);
  const capabilityToken = Hello.createCapabilityTokenVector(builder, this.capabilityToken);

  return Hello.createHello(builder,
    this.protoVersion,
    connectionId,
    capabilityToken
  );
}
}
