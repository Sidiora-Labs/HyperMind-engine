// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class AsOf {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):AsOf {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsAsOf(bb:flatbuffers.ByteBuffer, obj?:AsOf):AsOf {
  return (obj || new AsOf()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsAsOf(bb:flatbuffers.ByteBuffer, obj?:AsOf):AsOf {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new AsOf()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

beliefType():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

canonicalIdentity():string|null
canonicalIdentity(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
canonicalIdentity(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

validTimeNs():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

transactionLsn():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

knownLsn():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

static startAsOf(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addBeliefType(builder:flatbuffers.Builder, beliefType:number) {
  builder.addFieldInt8(0, beliefType, 0);
}

static addCanonicalIdentity(builder:flatbuffers.Builder, canonicalIdentityOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, canonicalIdentityOffset, 0);
}

static addValidTimeNs(builder:flatbuffers.Builder, validTimeNs:bigint) {
  builder.addFieldInt64(2, validTimeNs, BigInt('0'));
}

static addTransactionLsn(builder:flatbuffers.Builder, transactionLsn:bigint) {
  builder.addFieldInt64(3, transactionLsn, BigInt('0'));
}

static addKnownLsn(builder:flatbuffers.Builder, knownLsn:bigint) {
  builder.addFieldInt64(4, knownLsn, BigInt('0'));
}

static endAsOf(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 6) // canonical_identity
  return offset;
}

static createAsOf(builder:flatbuffers.Builder, beliefType:number, canonicalIdentityOffset:flatbuffers.Offset, validTimeNs:bigint, transactionLsn:bigint, knownLsn:bigint):flatbuffers.Offset {
  AsOf.startAsOf(builder);
  AsOf.addBeliefType(builder, beliefType);
  AsOf.addCanonicalIdentity(builder, canonicalIdentityOffset);
  AsOf.addValidTimeNs(builder, validTimeNs);
  AsOf.addTransactionLsn(builder, transactionLsn);
  AsOf.addKnownLsn(builder, knownLsn);
  return AsOf.endAsOf(builder);
}

unpack(): AsOfT {
  return new AsOfT(
    this.beliefType(),
    this.canonicalIdentity(),
    this.validTimeNs(),
    this.transactionLsn(),
    this.knownLsn()
  );
}


unpackTo(_o: AsOfT): void {
  _o.beliefType = this.beliefType();
  _o.canonicalIdentity = this.canonicalIdentity();
  _o.validTimeNs = this.validTimeNs();
  _o.transactionLsn = this.transactionLsn();
  _o.knownLsn = this.knownLsn();
}
}

export class AsOfT {
constructor(
  public beliefType: number = 0,
  public canonicalIdentity: string|Uint8Array|null = null,
  public validTimeNs: bigint = BigInt('0'),
  public transactionLsn: bigint = BigInt('0'),
  public knownLsn: bigint = BigInt('0')
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const canonicalIdentity = (this.canonicalIdentity !== null ? builder.createString(this.canonicalIdentity!) : 0);

  return AsOf.createAsOf(builder,
    this.beliefType,
    canonicalIdentity,
    this.validTimeNs,
    this.transactionLsn,
    this.knownLsn
  );
}
}
