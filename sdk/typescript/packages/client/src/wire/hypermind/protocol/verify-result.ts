// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class VerifyResult {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):VerifyResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsVerifyResult(bb:flatbuffers.ByteBuffer, obj?:VerifyResult):VerifyResult {
  return (obj || new VerifyResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsVerifyResult(bb:flatbuffers.ByteBuffer, obj?:VerifyResult):VerifyResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new VerifyResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

actor():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

root(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

rootLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

rootArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

leafCount():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

lastCheckpointLsn():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

verified():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startVerifyResult(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addActor(builder:flatbuffers.Builder, actor:number) {
  builder.addFieldInt16(0, actor, 0);
}

static addRoot(builder:flatbuffers.Builder, rootOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, rootOffset, 0);
}

static createRootVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startRootVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addLeafCount(builder:flatbuffers.Builder, leafCount:bigint) {
  builder.addFieldInt64(2, leafCount, BigInt('0'));
}

static addLastCheckpointLsn(builder:flatbuffers.Builder, lastCheckpointLsn:bigint) {
  builder.addFieldInt64(3, lastCheckpointLsn, BigInt('0'));
}

static addVerified(builder:flatbuffers.Builder, verified:boolean) {
  builder.addFieldInt8(4, +verified, +false);
}

static endVerifyResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 6) // root
  return offset;
}

static createVerifyResult(builder:flatbuffers.Builder, actor:number, rootOffset:flatbuffers.Offset, leafCount:bigint, lastCheckpointLsn:bigint, verified:boolean):flatbuffers.Offset {
  VerifyResult.startVerifyResult(builder);
  VerifyResult.addActor(builder, actor);
  VerifyResult.addRoot(builder, rootOffset);
  VerifyResult.addLeafCount(builder, leafCount);
  VerifyResult.addLastCheckpointLsn(builder, lastCheckpointLsn);
  VerifyResult.addVerified(builder, verified);
  return VerifyResult.endVerifyResult(builder);
}

unpack(): VerifyResultT {
  return new VerifyResultT(
    this.actor(),
    this.bb!.createScalarList(this.root.bind(this), this.rootLength()),
    this.leafCount(),
    this.lastCheckpointLsn(),
    this.verified()
  );
}


unpackTo(_o: VerifyResultT): void {
  _o.actor = this.actor();
  _o.root = this.bb!.createScalarList(this.root.bind(this), this.rootLength());
  _o.leafCount = this.leafCount();
  _o.lastCheckpointLsn = this.lastCheckpointLsn();
  _o.verified = this.verified();
}
}

export class VerifyResultT {
constructor(
  public actor: number = 0,
  public root: (number)[] = [],
  public leafCount: bigint = BigInt('0'),
  public lastCheckpointLsn: bigint = BigInt('0'),
  public verified: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const root = VerifyResult.createRootVector(builder, this.root);

  return VerifyResult.createVerifyResult(builder,
    this.actor,
    root,
    this.leafCount,
    this.lastCheckpointLsn,
    this.verified
  );
}
}
