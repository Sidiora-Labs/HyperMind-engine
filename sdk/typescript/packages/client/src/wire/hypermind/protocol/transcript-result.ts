// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { FrameRecord, FrameRecordT } from '../../hypermind/protocol/frame-record';


export class TranscriptResult {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TranscriptResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTranscriptResult(bb:flatbuffers.ByteBuffer, obj?:TranscriptResult):TranscriptResult {
  return (obj || new TranscriptResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTranscriptResult(bb:flatbuffers.ByteBuffer, obj?:TranscriptResult):TranscriptResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TranscriptResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

records(index: number, obj?:FrameRecord):FrameRecord|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new FrameRecord()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

recordsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

truncated():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startTranscriptResult(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addRecords(builder:flatbuffers.Builder, recordsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, recordsOffset, 0);
}

static createRecordsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startRecordsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addTruncated(builder:flatbuffers.Builder, truncated:boolean) {
  builder.addFieldInt8(1, +truncated, +false);
}

static endTranscriptResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // records
  return offset;
}

static createTranscriptResult(builder:flatbuffers.Builder, recordsOffset:flatbuffers.Offset, truncated:boolean):flatbuffers.Offset {
  TranscriptResult.startTranscriptResult(builder);
  TranscriptResult.addRecords(builder, recordsOffset);
  TranscriptResult.addTruncated(builder, truncated);
  return TranscriptResult.endTranscriptResult(builder);
}

unpack(): TranscriptResultT {
  return new TranscriptResultT(
    this.bb!.createObjList(this.records.bind(this), this.recordsLength()),
    this.truncated()
  );
}


unpackTo(_o: TranscriptResultT): void {
  _o.records = this.bb!.createObjList(this.records.bind(this), this.recordsLength());
  _o.truncated = this.truncated();
}
}

export class TranscriptResultT {
constructor(
  public records: (FrameRecordT)[] = [],
  public truncated: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const records = TranscriptResult.createRecordsVector(builder, builder.createObjectOffsetList(this.records));

  return TranscriptResult.createTranscriptResult(builder,
    records,
    this.truncated
  );
}
}
