// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { TemporalWindowRecord, TemporalWindowRecordT } from '../../hypermind/protocol/temporal-window-record';


export class RecallResult {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):RecallResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRecallResult(bb:flatbuffers.ByteBuffer, obj?:RecallResult):RecallResult {
  return (obj || new RecallResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRecallResult(bb:flatbuffers.ByteBuffer, obj?:RecallResult):RecallResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new RecallResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

windows(index: number, obj?:TemporalWindowRecord):TemporalWindowRecord|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new TemporalWindowRecord()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

windowsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

members(index: number):bigint|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : BigInt(0);
}

membersLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startRecallResult(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addWindows(builder:flatbuffers.Builder, windowsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, windowsOffset, 0);
}

static createWindowsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startWindowsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addMembers(builder:flatbuffers.Builder, membersOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, membersOffset, 0);
}

static createMembersVector(builder:flatbuffers.Builder, data:bigint[]):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt64(data[i]!);
  }
  return builder.endVector();
}

static startMembersVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static endRecallResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createRecallResult(builder:flatbuffers.Builder, windowsOffset:flatbuffers.Offset, membersOffset:flatbuffers.Offset):flatbuffers.Offset {
  RecallResult.startRecallResult(builder);
  RecallResult.addWindows(builder, windowsOffset);
  RecallResult.addMembers(builder, membersOffset);
  return RecallResult.endRecallResult(builder);
}

unpack(): RecallResultT {
  return new RecallResultT(
    this.bb!.createObjList(this.windows.bind(this), this.windowsLength()),
    this.bb!.createScalarList(this.members.bind(this), this.membersLength())
  );
}


unpackTo(_o: RecallResultT): void {
  _o.windows = this.bb!.createObjList(this.windows.bind(this), this.windowsLength());
  _o.members = this.bb!.createScalarList(this.members.bind(this), this.membersLength());
}
}

export class RecallResultT {
constructor(
  public windows: (TemporalWindowRecordT)[] = [],
  public members: (bigint)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const windows = RecallResult.createWindowsVector(builder, builder.createObjectOffsetList(this.windows));
  const members = RecallResult.createMembersVector(builder, this.members);

  return RecallResult.createRecallResult(builder,
    windows,
    members
  );
}
}
