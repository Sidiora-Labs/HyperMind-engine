// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { Event, EventT } from '../../hypermind/protocol/event';
import { Hello, HelloT } from '../../hypermind/protocol/hello';
import { Request, RequestT } from '../../hypermind/protocol/request';
import { Response, ResponseT } from '../../hypermind/protocol/response';
import { Welcome, WelcomeT } from '../../hypermind/protocol/welcome';
import { WirePayload, unionToWirePayload, unionListToWirePayload } from '../../hypermind/protocol/wire-payload';


export class WireEnvelope {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):WireEnvelope {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsWireEnvelope(bb:flatbuffers.ByteBuffer, obj?:WireEnvelope):WireEnvelope {
  return (obj || new WireEnvelope()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsWireEnvelope(bb:flatbuffers.ByteBuffer, obj?:WireEnvelope):WireEnvelope {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new WireEnvelope()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('NCPR');
}

protoVersion():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

payloadType():WirePayload {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : WirePayload.NONE;
}

payload<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

static startWireEnvelope(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addProtoVersion(builder:flatbuffers.Builder, protoVersion:number) {
  builder.addFieldInt16(0, protoVersion, 0);
}

static addPayloadType(builder:flatbuffers.Builder, payloadType:WirePayload) {
  builder.addFieldInt8(1, payloadType, WirePayload.NONE);
}

static addPayload(builder:flatbuffers.Builder, payloadOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, payloadOffset, 0);
}

static endWireEnvelope(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 8) // payload
  return offset;
}

static finishWireEnvelopeBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'NCPR');
}

static finishSizePrefixedWireEnvelopeBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'NCPR', true);
}

static createWireEnvelope(builder:flatbuffers.Builder, protoVersion:number, payloadType:WirePayload, payloadOffset:flatbuffers.Offset):flatbuffers.Offset {
  WireEnvelope.startWireEnvelope(builder);
  WireEnvelope.addProtoVersion(builder, protoVersion);
  WireEnvelope.addPayloadType(builder, payloadType);
  WireEnvelope.addPayload(builder, payloadOffset);
  return WireEnvelope.endWireEnvelope(builder);
}

unpack(): WireEnvelopeT {
  return new WireEnvelopeT(
    this.protoVersion(),
    this.payloadType(),
    (() => {
      let temp = unionToWirePayload(this.payloadType(), this.payload.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })()
  );
}


unpackTo(_o: WireEnvelopeT): void {
  _o.protoVersion = this.protoVersion();
  _o.payloadType = this.payloadType();
  _o.payload = (() => {
      let temp = unionToWirePayload(this.payloadType(), this.payload.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })();
}
}

export class WireEnvelopeT {
constructor(
  public protoVersion: number = 0,
  public payloadType: WirePayload = WirePayload.NONE,
  public payload: EventT|HelloT|RequestT|ResponseT|WelcomeT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const payload = builder.createObjectOffset(this.payload);

  return WireEnvelope.createWireEnvelope(builder,
    this.protoVersion,
    this.payloadType,
    payload
  );
}
}
