// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { Activate, ActivateT } from '../../hypermind/protocol/activate';
import { Append, AppendT } from '../../hypermind/protocol/append';
import { AsOf, AsOfT } from '../../hypermind/protocol/as-of';
import { Attest, AttestT } from '../../hypermind/protocol/attest';
import { Checkpoint, CheckpointT } from '../../hypermind/protocol/checkpoint';
import { CryptoDelete, CryptoDeleteT } from '../../hypermind/protocol/crypto-delete';
import { Health, HealthT } from '../../hypermind/protocol/health';
import { LatencyHistograms, LatencyHistogramsT } from '../../hypermind/protocol/latency-histograms';
import { LatestCheckpoint, LatestCheckpointT } from '../../hypermind/protocol/latest-checkpoint';
import { RebuildProjection, RebuildProjectionT } from '../../hypermind/protocol/rebuild-projection';
import { Recall, RecallT } from '../../hypermind/protocol/recall';
import { RequestPayload, unionToRequestPayload, unionListToRequestPayload } from '../../hypermind/protocol/request-payload';
import { Stats, StatsT } from '../../hypermind/protocol/stats';
import { Subscribe, SubscribeT } from '../../hypermind/protocol/subscribe';
import { Transcript, TranscriptT } from '../../hypermind/protocol/transcript';
import { VerifyStatus, VerifyStatusT } from '../../hypermind/protocol/verify-status';


export class Request {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Request {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRequest(bb:flatbuffers.ByteBuffer, obj?:Request):Request {
  return (obj || new Request()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRequest(bb:flatbuffers.ByteBuffer, obj?:Request):Request {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Request()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

requestId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

payloadType():RequestPayload {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : RequestPayload.NONE;
}

payload<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

static startRequest(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addRequestId(builder:flatbuffers.Builder, requestId:bigint) {
  builder.addFieldInt64(0, requestId, BigInt('0'));
}

static addPayloadType(builder:flatbuffers.Builder, payloadType:RequestPayload) {
  builder.addFieldInt8(1, payloadType, RequestPayload.NONE);
}

static addPayload(builder:flatbuffers.Builder, payloadOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, payloadOffset, 0);
}

static endRequest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 8) // payload
  return offset;
}

static createRequest(builder:flatbuffers.Builder, requestId:bigint, payloadType:RequestPayload, payloadOffset:flatbuffers.Offset):flatbuffers.Offset {
  Request.startRequest(builder);
  Request.addRequestId(builder, requestId);
  Request.addPayloadType(builder, payloadType);
  Request.addPayload(builder, payloadOffset);
  return Request.endRequest(builder);
}

unpack(): RequestT {
  return new RequestT(
    this.requestId(),
    this.payloadType(),
    (() => {
      let temp = unionToRequestPayload(this.payloadType(), this.payload.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })()
  );
}


unpackTo(_o: RequestT): void {
  _o.requestId = this.requestId();
  _o.payloadType = this.payloadType();
  _o.payload = (() => {
      let temp = unionToRequestPayload(this.payloadType(), this.payload.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })();
}
}

export class RequestT {
constructor(
  public requestId: bigint = BigInt('0'),
  public payloadType: RequestPayload = RequestPayload.NONE,
  public payload: ActivateT|AppendT|AsOfT|AttestT|CheckpointT|CryptoDeleteT|HealthT|LatencyHistogramsT|LatestCheckpointT|RebuildProjectionT|RecallT|StatsT|SubscribeT|TranscriptT|VerifyStatusT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const payload = builder.createObjectOffset(this.payload);

  return Request.createRequest(builder,
    this.requestId,
    this.payloadType,
    payload
  );
}
}
