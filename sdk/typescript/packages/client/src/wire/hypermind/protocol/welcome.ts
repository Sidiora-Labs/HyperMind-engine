// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class Welcome {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Welcome {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsWelcome(bb:flatbuffers.ByteBuffer, obj?:Welcome):Welcome {
  return (obj || new Welcome()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsWelcome(bb:flatbuffers.ByteBuffer, obj?:Welcome):Welcome {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Welcome()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

protoVersion():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

actorNs():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

admin():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

maximumFrameBytes():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

maximumBatchEvents():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

maximumSubscriptions():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

nextClientSeq():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

static startWelcome(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addProtoVersion(builder:flatbuffers.Builder, protoVersion:number) {
  builder.addFieldInt16(0, protoVersion, 0);
}

static addActorNs(builder:flatbuffers.Builder, actorNs:number) {
  builder.addFieldInt16(1, actorNs, 0);
}

static addAdmin(builder:flatbuffers.Builder, admin:boolean) {
  builder.addFieldInt8(2, +admin, +false);
}

static addMaximumFrameBytes(builder:flatbuffers.Builder, maximumFrameBytes:number) {
  builder.addFieldInt32(3, maximumFrameBytes, 0);
}

static addMaximumBatchEvents(builder:flatbuffers.Builder, maximumBatchEvents:number) {
  builder.addFieldInt32(4, maximumBatchEvents, 0);
}

static addMaximumSubscriptions(builder:flatbuffers.Builder, maximumSubscriptions:number) {
  builder.addFieldInt32(5, maximumSubscriptions, 0);
}

static addNextClientSeq(builder:flatbuffers.Builder, nextClientSeq:bigint) {
  builder.addFieldInt64(6, nextClientSeq, BigInt('0'));
}

static endWelcome(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createWelcome(builder:flatbuffers.Builder, protoVersion:number, actorNs:number, admin:boolean, maximumFrameBytes:number, maximumBatchEvents:number, maximumSubscriptions:number, nextClientSeq:bigint):flatbuffers.Offset {
  Welcome.startWelcome(builder);
  Welcome.addProtoVersion(builder, protoVersion);
  Welcome.addActorNs(builder, actorNs);
  Welcome.addAdmin(builder, admin);
  Welcome.addMaximumFrameBytes(builder, maximumFrameBytes);
  Welcome.addMaximumBatchEvents(builder, maximumBatchEvents);
  Welcome.addMaximumSubscriptions(builder, maximumSubscriptions);
  Welcome.addNextClientSeq(builder, nextClientSeq);
  return Welcome.endWelcome(builder);
}

unpack(): WelcomeT {
  return new WelcomeT(
    this.protoVersion(),
    this.actorNs(),
    this.admin(),
    this.maximumFrameBytes(),
    this.maximumBatchEvents(),
    this.maximumSubscriptions(),
    this.nextClientSeq()
  );
}


unpackTo(_o: WelcomeT): void {
  _o.protoVersion = this.protoVersion();
  _o.actorNs = this.actorNs();
  _o.admin = this.admin();
  _o.maximumFrameBytes = this.maximumFrameBytes();
  _o.maximumBatchEvents = this.maximumBatchEvents();
  _o.maximumSubscriptions = this.maximumSubscriptions();
  _o.nextClientSeq = this.nextClientSeq();
}
}

export class WelcomeT {
constructor(
  public protoVersion: number = 0,
  public actorNs: number = 0,
  public admin: boolean = false,
  public maximumFrameBytes: number = 0,
  public maximumBatchEvents: number = 0,
  public maximumSubscriptions: number = 0,
  public nextClientSeq: bigint = BigInt('0')
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return Welcome.createWelcome(builder,
    this.protoVersion,
    this.actorNs,
    this.admin,
    this.maximumFrameBytes,
    this.maximumBatchEvents,
    this.maximumSubscriptions,
    this.nextClientSeq
  );
}
}
