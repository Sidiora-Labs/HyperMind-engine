// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { RecallMode } from '../../hypermind/protocol/recall-mode';


export class Recall {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Recall {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRecall(bb:flatbuffers.ByteBuffer, obj?:Recall):Recall {
  return (obj || new Recall()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRecall(bb:flatbuffers.ByteBuffer, obj?:Recall):Recall {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Recall()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

query(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

queryLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

queryArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

limit():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

mode():RecallMode {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : RecallMode.list_windows;
}

level():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

startNs():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

endNs():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

static startRecall(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addQuery(builder:flatbuffers.Builder, queryOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, queryOffset, 0);
}

static createQueryVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startQueryVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addLimit(builder:flatbuffers.Builder, limit:number) {
  builder.addFieldInt32(1, limit, 0);
}

static addMode(builder:flatbuffers.Builder, mode:RecallMode) {
  builder.addFieldInt8(2, mode, RecallMode.list_windows);
}

static addLevel(builder:flatbuffers.Builder, level:number) {
  builder.addFieldInt8(3, level, 0);
}

static addStartNs(builder:flatbuffers.Builder, startNs:bigint) {
  builder.addFieldInt64(4, startNs, BigInt('0'));
}

static addEndNs(builder:flatbuffers.Builder, endNs:bigint) {
  builder.addFieldInt64(5, endNs, BigInt('0'));
}

static endRecall(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // query
  return offset;
}

static createRecall(builder:flatbuffers.Builder, queryOffset:flatbuffers.Offset, limit:number, mode:RecallMode, level:number, startNs:bigint, endNs:bigint):flatbuffers.Offset {
  Recall.startRecall(builder);
  Recall.addQuery(builder, queryOffset);
  Recall.addLimit(builder, limit);
  Recall.addMode(builder, mode);
  Recall.addLevel(builder, level);
  Recall.addStartNs(builder, startNs);
  Recall.addEndNs(builder, endNs);
  return Recall.endRecall(builder);
}

unpack(): RecallT {
  return new RecallT(
    this.bb!.createScalarList(this.query.bind(this), this.queryLength()),
    this.limit(),
    this.mode(),
    this.level(),
    this.startNs(),
    this.endNs()
  );
}


unpackTo(_o: RecallT): void {
  _o.query = this.bb!.createScalarList(this.query.bind(this), this.queryLength());
  _o.limit = this.limit();
  _o.mode = this.mode();
  _o.level = this.level();
  _o.startNs = this.startNs();
  _o.endNs = this.endNs();
}
}

export class RecallT {
constructor(
  public query: (number)[] = [],
  public limit: number = 0,
  public mode: RecallMode = RecallMode.list_windows,
  public level: number = 0,
  public startNs: bigint = BigInt('0'),
  public endNs: bigint = BigInt('0')
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const query = Recall.createQueryVector(builder, this.query);

  return Recall.createRecall(builder,
    query,
    this.limit,
    this.mode,
    this.level,
    this.startNs,
    this.endNs
  );
}
}
