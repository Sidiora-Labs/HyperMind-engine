// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



/**
 * Pushes one committed ledger event to a subscriber.
 */
export class Event {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Event {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsEvent(bb:flatbuffers.ByteBuffer, obj?:Event):Event {
  return (obj || new Event()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsEvent(bb:flatbuffers.ByteBuffer, obj?:Event):Event {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Event()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

subscriptionId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

lsn():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

kind():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

wallTimestampNs():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

actor():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

conversation(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

conversationLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

conversationArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

payload(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

payloadLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

payloadArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startEvent(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addSubscriptionId(builder:flatbuffers.Builder, subscriptionId:bigint) {
  builder.addFieldInt64(0, subscriptionId, BigInt('0'));
}

static addLsn(builder:flatbuffers.Builder, lsn:bigint) {
  builder.addFieldInt64(1, lsn, BigInt('0'));
}

static addKind(builder:flatbuffers.Builder, kind:number) {
  builder.addFieldInt8(2, kind, 0);
}

static addWallTimestampNs(builder:flatbuffers.Builder, wallTimestampNs:bigint) {
  builder.addFieldInt64(3, wallTimestampNs, BigInt('0'));
}

static addActor(builder:flatbuffers.Builder, actor:number) {
  builder.addFieldInt16(4, actor, 0);
}

static addConversation(builder:flatbuffers.Builder, conversationOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, conversationOffset, 0);
}

static createConversationVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startConversationVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addPayload(builder:flatbuffers.Builder, payloadOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, payloadOffset, 0);
}

static createPayloadVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startPayloadVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endEvent(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 14) // conversation
  builder.requiredField(offset, 16) // payload
  return offset;
}

static createEvent(builder:flatbuffers.Builder, subscriptionId:bigint, lsn:bigint, kind:number, wallTimestampNs:bigint, actor:number, conversationOffset:flatbuffers.Offset, payloadOffset:flatbuffers.Offset):flatbuffers.Offset {
  Event.startEvent(builder);
  Event.addSubscriptionId(builder, subscriptionId);
  Event.addLsn(builder, lsn);
  Event.addKind(builder, kind);
  Event.addWallTimestampNs(builder, wallTimestampNs);
  Event.addActor(builder, actor);
  Event.addConversation(builder, conversationOffset);
  Event.addPayload(builder, payloadOffset);
  return Event.endEvent(builder);
}

unpack(): EventT {
  return new EventT(
    this.subscriptionId(),
    this.lsn(),
    this.kind(),
    this.wallTimestampNs(),
    this.actor(),
    this.bb!.createScalarList(this.conversation.bind(this), this.conversationLength()),
    this.bb!.createScalarList(this.payload.bind(this), this.payloadLength())
  );
}


unpackTo(_o: EventT): void {
  _o.subscriptionId = this.subscriptionId();
  _o.lsn = this.lsn();
  _o.kind = this.kind();
  _o.wallTimestampNs = this.wallTimestampNs();
  _o.actor = this.actor();
  _o.conversation = this.bb!.createScalarList(this.conversation.bind(this), this.conversationLength());
  _o.payload = this.bb!.createScalarList(this.payload.bind(this), this.payloadLength());
}
}

export class EventT {
constructor(
  public subscriptionId: bigint = BigInt('0'),
  public lsn: bigint = BigInt('0'),
  public kind: number = 0,
  public wallTimestampNs: bigint = BigInt('0'),
  public actor: number = 0,
  public conversation: (number)[] = [],
  public payload: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const conversation = Event.createConversationVector(builder, this.conversation);
  const payload = Event.createPayloadVector(builder, this.payload);

  return Event.createEvent(builder,
    this.subscriptionId,
    this.lsn,
    this.kind,
    this.wallTimestampNs,
    this.actor,
    conversation,
    payload
  );
}
}
