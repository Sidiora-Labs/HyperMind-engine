// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { ProjectionStat, ProjectionStatT } from '../../hypermind/protocol/projection-stat';


export class StatsResult {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):StatsResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsStatsResult(bb:flatbuffers.ByteBuffer, obj?:StatsResult):StatsResult {
  return (obj || new StatsResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsStatsResult(bb:flatbuffers.ByteBuffer, obj?:StatsResult):StatsResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new StatsResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

actor():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

logEvents():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

logBytes():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

projectionStats(index: number, obj?:ProjectionStat):ProjectionStat|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new ProjectionStat()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

projectionStatsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startStatsResult(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addActor(builder:flatbuffers.Builder, actor:number) {
  builder.addFieldInt16(0, actor, 0);
}

static addLogEvents(builder:flatbuffers.Builder, logEvents:bigint) {
  builder.addFieldInt64(1, logEvents, BigInt('0'));
}

static addLogBytes(builder:flatbuffers.Builder, logBytes:bigint) {
  builder.addFieldInt64(2, logBytes, BigInt('0'));
}

static addProjectionStats(builder:flatbuffers.Builder, projectionStatsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, projectionStatsOffset, 0);
}

static createProjectionStatsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startProjectionStatsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endStatsResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 10) // projection_stats
  return offset;
}

static createStatsResult(builder:flatbuffers.Builder, actor:number, logEvents:bigint, logBytes:bigint, projectionStatsOffset:flatbuffers.Offset):flatbuffers.Offset {
  StatsResult.startStatsResult(builder);
  StatsResult.addActor(builder, actor);
  StatsResult.addLogEvents(builder, logEvents);
  StatsResult.addLogBytes(builder, logBytes);
  StatsResult.addProjectionStats(builder, projectionStatsOffset);
  return StatsResult.endStatsResult(builder);
}

unpack(): StatsResultT {
  return new StatsResultT(
    this.actor(),
    this.logEvents(),
    this.logBytes(),
    this.bb!.createObjList(this.projectionStats.bind(this), this.projectionStatsLength())
  );
}


unpackTo(_o: StatsResultT): void {
  _o.actor = this.actor();
  _o.logEvents = this.logEvents();
  _o.logBytes = this.logBytes();
  _o.projectionStats = this.bb!.createObjList(this.projectionStats.bind(this), this.projectionStatsLength());
}
}

export class StatsResultT {
constructor(
  public actor: number = 0,
  public logEvents: bigint = BigInt('0'),
  public logBytes: bigint = BigInt('0'),
  public projectionStats: (ProjectionStatT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const projectionStats = StatsResult.createProjectionStatsVector(builder, builder.createObjectOffsetList(this.projectionStats));

  return StatsResult.createStatsResult(builder,
    this.actor,
    this.logEvents,
    this.logBytes,
    projectionStats
  );
}
}
