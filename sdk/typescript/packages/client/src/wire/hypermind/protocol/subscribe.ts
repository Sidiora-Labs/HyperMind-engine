// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



/**
 * Streams committed events after an optional conversation and LSN cursor.
 */
export class Subscribe {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Subscribe {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSubscribe(bb:flatbuffers.ByteBuffer, obj?:Subscribe):Subscribe {
  return (obj || new Subscribe()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSubscribe(bb:flatbuffers.ByteBuffer, obj?:Subscribe):Subscribe {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Subscribe()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

conversation(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

conversationLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

conversationArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

sinceLsn():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

static startSubscribe(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addConversation(builder:flatbuffers.Builder, conversationOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, conversationOffset, 0);
}

static createConversationVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startConversationVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addSinceLsn(builder:flatbuffers.Builder, sinceLsn:bigint) {
  builder.addFieldInt64(1, sinceLsn, BigInt('0'));
}

static endSubscribe(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSubscribe(builder:flatbuffers.Builder, conversationOffset:flatbuffers.Offset, sinceLsn:bigint):flatbuffers.Offset {
  Subscribe.startSubscribe(builder);
  Subscribe.addConversation(builder, conversationOffset);
  Subscribe.addSinceLsn(builder, sinceLsn);
  return Subscribe.endSubscribe(builder);
}

unpack(): SubscribeT {
  return new SubscribeT(
    this.bb!.createScalarList(this.conversation.bind(this), this.conversationLength()),
    this.sinceLsn()
  );
}


unpackTo(_o: SubscribeT): void {
  _o.conversation = this.bb!.createScalarList(this.conversation.bind(this), this.conversationLength());
  _o.sinceLsn = this.sinceLsn();
}
}

export class SubscribeT {
constructor(
  public conversation: (number)[] = [],
  public sinceLsn: bigint = BigInt('0')
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const conversation = Subscribe.createConversationVector(builder, this.conversation);

  return Subscribe.createSubscribe(builder,
    conversation,
    this.sinceLsn
  );
}
}
