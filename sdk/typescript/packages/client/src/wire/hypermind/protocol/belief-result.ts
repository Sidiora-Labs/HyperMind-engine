// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { BeliefConflictRecord, BeliefConflictRecordT } from '../../hypermind/protocol/belief-conflict-record';
import { BeliefProvenanceRecord, BeliefProvenanceRecordT } from '../../hypermind/protocol/belief-provenance-record';


export class BeliefResult {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):BeliefResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsBeliefResult(bb:flatbuffers.ByteBuffer, obj?:BeliefResult):BeliefResult {
  return (obj || new BeliefResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsBeliefResult(bb:flatbuffers.ByteBuffer, obj?:BeliefResult):BeliefResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new BeliefResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

present():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

beliefType():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

beliefId(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

beliefIdLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

beliefIdArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

canonicalIdentity():string|null
canonicalIdentity(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
canonicalIdentity(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

conflictDomain():string|null
conflictDomain(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
conflictDomain(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

value(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

valueLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

valueArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

claim():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

validFromNs():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

validToNs():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

transactionLsn():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

version():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

supersedesVersion():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

provenance(index: number, obj?:BeliefProvenanceRecord):BeliefProvenanceRecord|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? (obj || new BeliefProvenanceRecord()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

provenanceLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

conflictEdges(index: number, obj?:BeliefConflictRecord):BeliefConflictRecord|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? (obj || new BeliefConflictRecord()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

conflictEdgesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

tombstoned():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startBeliefResult(builder:flatbuffers.Builder) {
  builder.startObject(15);
}

static addPresent(builder:flatbuffers.Builder, present:boolean) {
  builder.addFieldInt8(0, +present, +false);
}

static addBeliefType(builder:flatbuffers.Builder, beliefType:number) {
  builder.addFieldInt8(1, beliefType, 0);
}

static addBeliefId(builder:flatbuffers.Builder, beliefIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, beliefIdOffset, 0);
}

static createBeliefIdVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startBeliefIdVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addCanonicalIdentity(builder:flatbuffers.Builder, canonicalIdentityOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, canonicalIdentityOffset, 0);
}

static addConflictDomain(builder:flatbuffers.Builder, conflictDomainOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, conflictDomainOffset, 0);
}

static addValue(builder:flatbuffers.Builder, valueOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, valueOffset, 0);
}

static createValueVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startValueVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addClaim(builder:flatbuffers.Builder, claim:number) {
  builder.addFieldInt8(6, claim, 0);
}

static addValidFromNs(builder:flatbuffers.Builder, validFromNs:bigint) {
  builder.addFieldInt64(7, validFromNs, BigInt('0'));
}

static addValidToNs(builder:flatbuffers.Builder, validToNs:bigint) {
  builder.addFieldInt64(8, validToNs, BigInt('0'));
}

static addTransactionLsn(builder:flatbuffers.Builder, transactionLsn:bigint) {
  builder.addFieldInt64(9, transactionLsn, BigInt('0'));
}

static addVersion(builder:flatbuffers.Builder, version:bigint) {
  builder.addFieldInt64(10, version, BigInt('0'));
}

static addSupersedesVersion(builder:flatbuffers.Builder, supersedesVersion:bigint) {
  builder.addFieldInt64(11, supersedesVersion, BigInt('0'));
}

static addProvenance(builder:flatbuffers.Builder, provenanceOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, provenanceOffset, 0);
}

static createProvenanceVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startProvenanceVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addConflictEdges(builder:flatbuffers.Builder, conflictEdgesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, conflictEdgesOffset, 0);
}

static createConflictEdgesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startConflictEdgesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addTombstoned(builder:flatbuffers.Builder, tombstoned:boolean) {
  builder.addFieldInt8(14, +tombstoned, +false);
}

static endBeliefResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createBeliefResult(builder:flatbuffers.Builder, present:boolean, beliefType:number, beliefIdOffset:flatbuffers.Offset, canonicalIdentityOffset:flatbuffers.Offset, conflictDomainOffset:flatbuffers.Offset, valueOffset:flatbuffers.Offset, claim:number, validFromNs:bigint, validToNs:bigint, transactionLsn:bigint, version:bigint, supersedesVersion:bigint, provenanceOffset:flatbuffers.Offset, conflictEdgesOffset:flatbuffers.Offset, tombstoned:boolean):flatbuffers.Offset {
  BeliefResult.startBeliefResult(builder);
  BeliefResult.addPresent(builder, present);
  BeliefResult.addBeliefType(builder, beliefType);
  BeliefResult.addBeliefId(builder, beliefIdOffset);
  BeliefResult.addCanonicalIdentity(builder, canonicalIdentityOffset);
  BeliefResult.addConflictDomain(builder, conflictDomainOffset);
  BeliefResult.addValue(builder, valueOffset);
  BeliefResult.addClaim(builder, claim);
  BeliefResult.addValidFromNs(builder, validFromNs);
  BeliefResult.addValidToNs(builder, validToNs);
  BeliefResult.addTransactionLsn(builder, transactionLsn);
  BeliefResult.addVersion(builder, version);
  BeliefResult.addSupersedesVersion(builder, supersedesVersion);
  BeliefResult.addProvenance(builder, provenanceOffset);
  BeliefResult.addConflictEdges(builder, conflictEdgesOffset);
  BeliefResult.addTombstoned(builder, tombstoned);
  return BeliefResult.endBeliefResult(builder);
}

unpack(): BeliefResultT {
  return new BeliefResultT(
    this.present(),
    this.beliefType(),
    this.bb!.createScalarList(this.beliefId.bind(this), this.beliefIdLength()),
    this.canonicalIdentity(),
    this.conflictDomain(),
    this.bb!.createScalarList(this.value.bind(this), this.valueLength()),
    this.claim(),
    this.validFromNs(),
    this.validToNs(),
    this.transactionLsn(),
    this.version(),
    this.supersedesVersion(),
    this.bb!.createObjList(this.provenance.bind(this), this.provenanceLength()),
    this.bb!.createObjList(this.conflictEdges.bind(this), this.conflictEdgesLength()),
    this.tombstoned()
  );
}


unpackTo(_o: BeliefResultT): void {
  _o.present = this.present();
  _o.beliefType = this.beliefType();
  _o.beliefId = this.bb!.createScalarList(this.beliefId.bind(this), this.beliefIdLength());
  _o.canonicalIdentity = this.canonicalIdentity();
  _o.conflictDomain = this.conflictDomain();
  _o.value = this.bb!.createScalarList(this.value.bind(this), this.valueLength());
  _o.claim = this.claim();
  _o.validFromNs = this.validFromNs();
  _o.validToNs = this.validToNs();
  _o.transactionLsn = this.transactionLsn();
  _o.version = this.version();
  _o.supersedesVersion = this.supersedesVersion();
  _o.provenance = this.bb!.createObjList(this.provenance.bind(this), this.provenanceLength());
  _o.conflictEdges = this.bb!.createObjList(this.conflictEdges.bind(this), this.conflictEdgesLength());
  _o.tombstoned = this.tombstoned();
}
}

export class BeliefResultT {
constructor(
  public present: boolean = false,
  public beliefType: number = 0,
  public beliefId: (number)[] = [],
  public canonicalIdentity: string|Uint8Array|null = null,
  public conflictDomain: string|Uint8Array|null = null,
  public value: (number)[] = [],
  public claim: number = 0,
  public validFromNs: bigint = BigInt('0'),
  public validToNs: bigint = BigInt('0'),
  public transactionLsn: bigint = BigInt('0'),
  public version: bigint = BigInt('0'),
  public supersedesVersion: bigint = BigInt('0'),
  public provenance: (BeliefProvenanceRecordT)[] = [],
  public conflictEdges: (BeliefConflictRecordT)[] = [],
  public tombstoned: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const beliefId = BeliefResult.createBeliefIdVector(builder, this.beliefId);
  const canonicalIdentity = (this.canonicalIdentity !== null ? builder.createString(this.canonicalIdentity!) : 0);
  const conflictDomain = (this.conflictDomain !== null ? builder.createString(this.conflictDomain!) : 0);
  const value = BeliefResult.createValueVector(builder, this.value);
  const provenance = BeliefResult.createProvenanceVector(builder, builder.createObjectOffsetList(this.provenance));
  const conflictEdges = BeliefResult.createConflictEdgesVector(builder, builder.createObjectOffsetList(this.conflictEdges));

  return BeliefResult.createBeliefResult(builder,
    this.present,
    this.beliefType,
    beliefId,
    canonicalIdentity,
    conflictDomain,
    value,
    this.claim,
    this.validFromNs,
    this.validToNs,
    this.transactionLsn,
    this.version,
    this.supersedesVersion,
    provenance,
    conflictEdges,
    this.tombstoned
  );
}
}
