// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { LatencyBucket, LatencyBucketT } from '../../hypermind/protocol/latency-bucket';


export class LatencyResult {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):LatencyResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsLatencyResult(bb:flatbuffers.ByteBuffer, obj?:LatencyResult):LatencyResult {
  return (obj || new LatencyResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsLatencyResult(bb:flatbuffers.ByteBuffer, obj?:LatencyResult):LatencyResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new LatencyResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

buckets(index: number, obj?:LatencyBucket):LatencyBucket|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new LatencyBucket()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

bucketsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startLatencyResult(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addBuckets(builder:flatbuffers.Builder, bucketsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, bucketsOffset, 0);
}

static createBucketsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startBucketsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endLatencyResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // buckets
  return offset;
}

static createLatencyResult(builder:flatbuffers.Builder, bucketsOffset:flatbuffers.Offset):flatbuffers.Offset {
  LatencyResult.startLatencyResult(builder);
  LatencyResult.addBuckets(builder, bucketsOffset);
  return LatencyResult.endLatencyResult(builder);
}

unpack(): LatencyResultT {
  return new LatencyResultT(
    this.bb!.createObjList(this.buckets.bind(this), this.bucketsLength())
  );
}


unpackTo(_o: LatencyResultT): void {
  _o.buckets = this.bb!.createObjList(this.buckets.bind(this), this.bucketsLength());
}
}

export class LatencyResultT {
constructor(
  public buckets: (LatencyBucketT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const buckets = LatencyResult.createBucketsVector(builder, builder.createObjectOffsetList(this.buckets));

  return LatencyResult.createLatencyResult(builder,
    buckets
  );
}
}
