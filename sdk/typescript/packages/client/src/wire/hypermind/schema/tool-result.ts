// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { ResultStatus } from '../../hypermind/schema/result-status';


export class ToolResult {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ToolResult {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsToolResult(bb:flatbuffers.ByteBuffer, obj?:ToolResult):ToolResult {
  return (obj || new ToolResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsToolResult(bb:flatbuffers.ByteBuffer, obj?:ToolResult):ToolResult {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ToolResult()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

callId(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

callIdLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

callIdArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

toolCallLsn():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

status():ResultStatus {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ResultStatus.ok;
}

result(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

resultLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

resultArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startToolResult(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addCallId(builder:flatbuffers.Builder, callIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, callIdOffset, 0);
}

static createCallIdVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startCallIdVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addToolCallLsn(builder:flatbuffers.Builder, toolCallLsn:bigint) {
  builder.addFieldInt64(1, toolCallLsn, BigInt('0'));
}

static addStatus(builder:flatbuffers.Builder, status:ResultStatus) {
  builder.addFieldInt8(2, status, ResultStatus.ok);
}

static addResult(builder:flatbuffers.Builder, resultOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, resultOffset, 0);
}

static createResultVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startResultVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endToolResult(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // call_id
  builder.requiredField(offset, 10) // result
  return offset;
}

static createToolResult(builder:flatbuffers.Builder, callIdOffset:flatbuffers.Offset, toolCallLsn:bigint, status:ResultStatus, resultOffset:flatbuffers.Offset):flatbuffers.Offset {
  ToolResult.startToolResult(builder);
  ToolResult.addCallId(builder, callIdOffset);
  ToolResult.addToolCallLsn(builder, toolCallLsn);
  ToolResult.addStatus(builder, status);
  ToolResult.addResult(builder, resultOffset);
  return ToolResult.endToolResult(builder);
}

unpack(): ToolResultT {
  return new ToolResultT(
    this.bb!.createScalarList(this.callId.bind(this), this.callIdLength()),
    this.toolCallLsn(),
    this.status(),
    this.bb!.createScalarList(this.result.bind(this), this.resultLength())
  );
}


unpackTo(_o: ToolResultT): void {
  _o.callId = this.bb!.createScalarList(this.callId.bind(this), this.callIdLength());
  _o.toolCallLsn = this.toolCallLsn();
  _o.status = this.status();
  _o.result = this.bb!.createScalarList(this.result.bind(this), this.resultLength());
}
}

export class ToolResultT {
constructor(
  public callId: (number)[] = [],
  public toolCallLsn: bigint = BigInt('0'),
  public status: ResultStatus = ResultStatus.ok,
  public result: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const callId = ToolResult.createCallIdVector(builder, this.callId);
  const result = ToolResult.createResultVector(builder, this.result);

  return ToolResult.createToolResult(builder,
    callId,
    this.toolCallLsn,
    this.status,
    result
  );
}
}
