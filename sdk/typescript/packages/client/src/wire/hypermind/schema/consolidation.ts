// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { Assertion, AssertionT } from '../../hypermind/schema/assertion';


export class Consolidation {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Consolidation {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsConsolidation(bb:flatbuffers.ByteBuffer, obj?:Consolidation):Consolidation {
  return (obj || new Consolidation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsConsolidation(bb:flatbuffers.ByteBuffer, obj?:Consolidation):Consolidation {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Consolidation()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

assertions(index: number, obj?:Assertion):Assertion|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new Assertion()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

assertionsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startConsolidation(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addAssertions(builder:flatbuffers.Builder, assertionsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, assertionsOffset, 0);
}

static createAssertionsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startAssertionsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endConsolidation(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // assertions
  return offset;
}

static createConsolidation(builder:flatbuffers.Builder, assertionsOffset:flatbuffers.Offset):flatbuffers.Offset {
  Consolidation.startConsolidation(builder);
  Consolidation.addAssertions(builder, assertionsOffset);
  return Consolidation.endConsolidation(builder);
}

unpack(): ConsolidationT {
  return new ConsolidationT(
    this.bb!.createObjList(this.assertions.bind(this), this.assertionsLength())
  );
}


unpackTo(_o: ConsolidationT): void {
  _o.assertions = this.bb!.createObjList(this.assertions.bind(this), this.assertionsLength());
}
}

export class ConsolidationT {
constructor(
  public assertions: (AssertionT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const assertions = Consolidation.createAssertionsVector(builder, builder.createObjectOffsetList(this.assertions));

  return Consolidation.createConsolidation(builder,
    assertions
  );
}
}
