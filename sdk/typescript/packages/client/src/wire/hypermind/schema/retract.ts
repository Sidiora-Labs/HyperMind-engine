// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { ProvenanceRange, ProvenanceRangeT } from '../../hypermind/schema/provenance-range';


export class Retract {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Retract {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRetract(bb:flatbuffers.ByteBuffer, obj?:Retract):Retract {
  return (obj || new Retract()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRetract(bb:flatbuffers.ByteBuffer, obj?:Retract):Retract {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Retract()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

beliefId(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

beliefIdLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

beliefIdArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

provenance(index: number, obj?:ProvenanceRange):ProvenanceRange|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new ProvenanceRange()).__init(this.bb!.__vector(this.bb_pos + offset) + index * 16, this.bb!) : null;
}

provenanceLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startRetract(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addBeliefId(builder:flatbuffers.Builder, beliefIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, beliefIdOffset, 0);
}

static createBeliefIdVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startBeliefIdVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addProvenance(builder:flatbuffers.Builder, provenanceOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, provenanceOffset, 0);
}

static startProvenanceVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(16, numElems, 8);
}

static endRetract(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // belief_id
  builder.requiredField(offset, 6) // provenance
  return offset;
}

static createRetract(builder:flatbuffers.Builder, beliefIdOffset:flatbuffers.Offset, provenanceOffset:flatbuffers.Offset):flatbuffers.Offset {
  Retract.startRetract(builder);
  Retract.addBeliefId(builder, beliefIdOffset);
  Retract.addProvenance(builder, provenanceOffset);
  return Retract.endRetract(builder);
}

unpack(): RetractT {
  return new RetractT(
    this.bb!.createScalarList(this.beliefId.bind(this), this.beliefIdLength()),
    this.bb!.createObjList(this.provenance.bind(this), this.provenanceLength())
  );
}


unpackTo(_o: RetractT): void {
  _o.beliefId = this.bb!.createScalarList(this.beliefId.bind(this), this.beliefIdLength());
  _o.provenance = this.bb!.createObjList(this.provenance.bind(this), this.provenanceLength());
}
}

export class RetractT {
constructor(
  public beliefId: (number)[] = [],
  public provenance: (ProvenanceRangeT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const beliefId = Retract.createBeliefIdVector(builder, this.beliefId);
  const provenance = builder.createStructOffsetList(this.provenance, Retract.startProvenanceVector);

  return Retract.createRetract(builder,
    beliefId,
    provenance
  );
}
}
