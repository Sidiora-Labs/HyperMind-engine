// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class Embedding {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Embedding {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsEmbedding(bb:flatbuffers.ByteBuffer, obj?:Embedding):Embedding {
  return (obj || new Embedding()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsEmbedding(bb:flatbuffers.ByteBuffer, obj?:Embedding):Embedding {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Embedding()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

targetLsn():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

dimension():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

quantized(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

quantizedLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

quantizedArray():Int8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Int8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

binaryPrefilter(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

binaryPrefilterLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

binaryPrefilterArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startEmbedding(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addTargetLsn(builder:flatbuffers.Builder, targetLsn:bigint) {
  builder.addFieldInt64(0, targetLsn, BigInt('0'));
}

static addDimension(builder:flatbuffers.Builder, dimension:number) {
  builder.addFieldInt32(1, dimension, 0);
}

static addQuantized(builder:flatbuffers.Builder, quantizedOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, quantizedOffset, 0);
}

static createQuantizedVector(builder:flatbuffers.Builder, data:number[]|Int8Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createQuantizedVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createQuantizedVector(builder:flatbuffers.Builder, data:number[]|Int8Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startQuantizedVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addBinaryPrefilter(builder:flatbuffers.Builder, binaryPrefilterOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, binaryPrefilterOffset, 0);
}

static createBinaryPrefilterVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startBinaryPrefilterVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endEmbedding(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 8) // quantized
  builder.requiredField(offset, 10) // binary_prefilter
  return offset;
}

static createEmbedding(builder:flatbuffers.Builder, targetLsn:bigint, dimension:number, quantizedOffset:flatbuffers.Offset, binaryPrefilterOffset:flatbuffers.Offset):flatbuffers.Offset {
  Embedding.startEmbedding(builder);
  Embedding.addTargetLsn(builder, targetLsn);
  Embedding.addDimension(builder, dimension);
  Embedding.addQuantized(builder, quantizedOffset);
  Embedding.addBinaryPrefilter(builder, binaryPrefilterOffset);
  return Embedding.endEmbedding(builder);
}

unpack(): EmbeddingT {
  return new EmbeddingT(
    this.targetLsn(),
    this.dimension(),
    this.bb!.createScalarList(this.quantized.bind(this), this.quantizedLength()),
    this.bb!.createScalarList(this.binaryPrefilter.bind(this), this.binaryPrefilterLength())
  );
}


unpackTo(_o: EmbeddingT): void {
  _o.targetLsn = this.targetLsn();
  _o.dimension = this.dimension();
  _o.quantized = this.bb!.createScalarList(this.quantized.bind(this), this.quantizedLength());
  _o.binaryPrefilter = this.bb!.createScalarList(this.binaryPrefilter.bind(this), this.binaryPrefilterLength());
}
}

export class EmbeddingT {
constructor(
  public targetLsn: bigint = BigInt('0'),
  public dimension: number = 0,
  public quantized: (number)[] = [],
  public binaryPrefilter: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const quantized = Embedding.createQuantizedVector(builder, this.quantized);
  const binaryPrefilter = Embedding.createBinaryPrefilterVector(builder, this.binaryPrefilter);

  return Embedding.createEmbedding(builder,
    this.targetLsn,
    this.dimension,
    quantized,
    binaryPrefilter
  );
}
}
