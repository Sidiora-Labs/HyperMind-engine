// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class Supervisor {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Supervisor {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSupervisor(bb:flatbuffers.ByteBuffer, obj?:Supervisor):Supervisor {
  return (obj || new Supervisor()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSupervisor(bb:flatbuffers.ByteBuffer, obj?:Supervisor):Supervisor {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Supervisor()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

code():string|null
code(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
code(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

evidence(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

evidenceLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

evidenceArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startSupervisor(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addCode(builder:flatbuffers.Builder, codeOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, codeOffset, 0);
}

static addEvidence(builder:flatbuffers.Builder, evidenceOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, evidenceOffset, 0);
}

static createEvidenceVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startEvidenceVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endSupervisor(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // code
  builder.requiredField(offset, 6) // evidence
  return offset;
}

static createSupervisor(builder:flatbuffers.Builder, codeOffset:flatbuffers.Offset, evidenceOffset:flatbuffers.Offset):flatbuffers.Offset {
  Supervisor.startSupervisor(builder);
  Supervisor.addCode(builder, codeOffset);
  Supervisor.addEvidence(builder, evidenceOffset);
  return Supervisor.endSupervisor(builder);
}

unpack(): SupervisorT {
  return new SupervisorT(
    this.code(),
    this.bb!.createScalarList(this.evidence.bind(this), this.evidenceLength())
  );
}


unpackTo(_o: SupervisorT): void {
  _o.code = this.code();
  _o.evidence = this.bb!.createScalarList(this.evidence.bind(this), this.evidenceLength());
}
}

export class SupervisorT {
constructor(
  public code: string|Uint8Array|null = null,
  public evidence: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const code = (this.code !== null ? builder.createString(this.code!) : 0);
  const evidence = Supervisor.createEvidenceVector(builder, this.evidence);

  return Supervisor.createSupervisor(builder,
    code,
    evidence
  );
}
}
