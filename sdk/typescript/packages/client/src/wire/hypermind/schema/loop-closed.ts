// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { LoopCloseReason } from '../../hypermind/schema/loop-close-reason';


export class LoopClosed {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):LoopClosed {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsLoopClosed(bb:flatbuffers.ByteBuffer, obj?:LoopClosed):LoopClosed {
  return (obj || new LoopClosed()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsLoopClosed(bb:flatbuffers.ByteBuffer, obj?:LoopClosed):LoopClosed {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new LoopClosed()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

loopId(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

loopIdLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

loopIdArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

reason():LoopCloseReason {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : LoopCloseReason.done;
}

cause(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

causeLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

causeArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

evidenceLsns(index: number):bigint|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint64(this.bb!.__vector(this.bb_pos + offset) + index * 8) : BigInt(0);
}

evidenceLsnsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startLoopClosed(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addLoopId(builder:flatbuffers.Builder, loopIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, loopIdOffset, 0);
}

static createLoopIdVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startLoopIdVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addReason(builder:flatbuffers.Builder, reason:LoopCloseReason) {
  builder.addFieldInt8(1, reason, LoopCloseReason.done);
}

static addCause(builder:flatbuffers.Builder, causeOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, causeOffset, 0);
}

static createCauseVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startCauseVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addEvidenceLsns(builder:flatbuffers.Builder, evidenceLsnsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, evidenceLsnsOffset, 0);
}

static createEvidenceLsnsVector(builder:flatbuffers.Builder, data:bigint[]):flatbuffers.Offset {
  builder.startVector(8, data.length, 8);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt64(data[i]!);
  }
  return builder.endVector();
}

static startEvidenceLsnsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 8);
}

static endLoopClosed(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // loop_id
  builder.requiredField(offset, 8) // cause
  return offset;
}

static createLoopClosed(builder:flatbuffers.Builder, loopIdOffset:flatbuffers.Offset, reason:LoopCloseReason, causeOffset:flatbuffers.Offset, evidenceLsnsOffset:flatbuffers.Offset):flatbuffers.Offset {
  LoopClosed.startLoopClosed(builder);
  LoopClosed.addLoopId(builder, loopIdOffset);
  LoopClosed.addReason(builder, reason);
  LoopClosed.addCause(builder, causeOffset);
  LoopClosed.addEvidenceLsns(builder, evidenceLsnsOffset);
  return LoopClosed.endLoopClosed(builder);
}

unpack(): LoopClosedT {
  return new LoopClosedT(
    this.bb!.createScalarList(this.loopId.bind(this), this.loopIdLength()),
    this.reason(),
    this.bb!.createScalarList(this.cause.bind(this), this.causeLength()),
    this.bb!.createScalarList(this.evidenceLsns.bind(this), this.evidenceLsnsLength())
  );
}


unpackTo(_o: LoopClosedT): void {
  _o.loopId = this.bb!.createScalarList(this.loopId.bind(this), this.loopIdLength());
  _o.reason = this.reason();
  _o.cause = this.bb!.createScalarList(this.cause.bind(this), this.causeLength());
  _o.evidenceLsns = this.bb!.createScalarList(this.evidenceLsns.bind(this), this.evidenceLsnsLength());
}
}

export class LoopClosedT {
constructor(
  public loopId: (number)[] = [],
  public reason: LoopCloseReason = LoopCloseReason.done,
  public cause: (number)[] = [],
  public evidenceLsns: (bigint)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const loopId = LoopClosed.createLoopIdVector(builder, this.loopId);
  const cause = LoopClosed.createCauseVector(builder, this.cause);
  const evidenceLsns = LoopClosed.createEvidenceLsnsVector(builder, this.evidenceLsns);

  return LoopClosed.createLoopClosed(builder,
    loopId,
    this.reason,
    cause,
    evidenceLsns
  );
}
}
