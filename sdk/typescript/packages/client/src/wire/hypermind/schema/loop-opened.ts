// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class LoopOpened {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):LoopOpened {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsLoopOpened(bb:flatbuffers.ByteBuffer, obj?:LoopOpened):LoopOpened {
  return (obj || new LoopOpened()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsLoopOpened(bb:flatbuffers.ByteBuffer, obj?:LoopOpened):LoopOpened {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new LoopOpened()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

loopId(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

loopIdLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

loopIdArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

objective(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

objectiveLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

objectiveArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startLoopOpened(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addLoopId(builder:flatbuffers.Builder, loopIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, loopIdOffset, 0);
}

static createLoopIdVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startLoopIdVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addObjective(builder:flatbuffers.Builder, objectiveOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, objectiveOffset, 0);
}

static createObjectiveVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startObjectiveVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endLoopOpened(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // loop_id
  builder.requiredField(offset, 6) // objective
  return offset;
}

static createLoopOpened(builder:flatbuffers.Builder, loopIdOffset:flatbuffers.Offset, objectiveOffset:flatbuffers.Offset):flatbuffers.Offset {
  LoopOpened.startLoopOpened(builder);
  LoopOpened.addLoopId(builder, loopIdOffset);
  LoopOpened.addObjective(builder, objectiveOffset);
  return LoopOpened.endLoopOpened(builder);
}

unpack(): LoopOpenedT {
  return new LoopOpenedT(
    this.bb!.createScalarList(this.loopId.bind(this), this.loopIdLength()),
    this.bb!.createScalarList(this.objective.bind(this), this.objectiveLength())
  );
}


unpackTo(_o: LoopOpenedT): void {
  _o.loopId = this.bb!.createScalarList(this.loopId.bind(this), this.loopIdLength());
  _o.objective = this.bb!.createScalarList(this.objective.bind(this), this.objectiveLength());
}
}

export class LoopOpenedT {
constructor(
  public loopId: (number)[] = [],
  public objective: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const loopId = LoopOpened.createLoopIdVector(builder, this.loopId);
  const objective = LoopOpened.createObjectiveVector(builder, this.objective);

  return LoopOpened.createLoopOpened(builder,
    loopId,
    objective
  );
}
}
