// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';



export class ToolCall {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ToolCall {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsToolCall(bb:flatbuffers.ByteBuffer, obj?:ToolCall):ToolCall {
  return (obj || new ToolCall()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsToolCall(bb:flatbuffers.ByteBuffer, obj?:ToolCall):ToolCall {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ToolCall()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

callId(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

callIdLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

callIdArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

toolName():string|null
toolName(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
toolName(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

arguments_(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

argumentsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

argumentsArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startToolCall(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addCallId(builder:flatbuffers.Builder, callIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, callIdOffset, 0);
}

static createCallIdVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startCallIdVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addToolName(builder:flatbuffers.Builder, toolNameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, toolNameOffset, 0);
}

static addArguments(builder:flatbuffers.Builder, arguments_Offset:flatbuffers.Offset) {
  builder.addFieldOffset(2, arguments_Offset, 0);
}

static createArgumentsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startArgumentsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static endToolCall(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 4) // call_id
  builder.requiredField(offset, 6) // tool_name
  builder.requiredField(offset, 8) // arguments
  return offset;
}

static createToolCall(builder:flatbuffers.Builder, callIdOffset:flatbuffers.Offset, toolNameOffset:flatbuffers.Offset, arguments_Offset:flatbuffers.Offset):flatbuffers.Offset {
  ToolCall.startToolCall(builder);
  ToolCall.addCallId(builder, callIdOffset);
  ToolCall.addToolName(builder, toolNameOffset);
  ToolCall.addArguments(builder, arguments_Offset);
  return ToolCall.endToolCall(builder);
}

unpack(): ToolCallT {
  return new ToolCallT(
    this.bb!.createScalarList(this.callId.bind(this), this.callIdLength()),
    this.toolName(),
    this.bb!.createScalarList(this.arguments_.bind(this), this.argumentsLength())
  );
}


unpackTo(_o: ToolCallT): void {
  _o.callId = this.bb!.createScalarList(this.callId.bind(this), this.callIdLength());
  _o.toolName = this.toolName();
  _o.arguments_ = this.bb!.createScalarList(this.arguments_.bind(this), this.argumentsLength());
}
}

export class ToolCallT {
constructor(
  public callId: (number)[] = [],
  public toolName: string|Uint8Array|null = null,
  public arguments_: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const callId = ToolCall.createCallIdVector(builder, this.callId);
  const toolName = (this.toolName !== null ? builder.createString(this.toolName!) : 0);
  const arguments_ = ToolCall.createArgumentsVector(builder, this.arguments_);

  return ToolCall.createToolCall(builder,
    callId,
    toolName,
    arguments_
  );
}
}
