// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { Approval, ApprovalT } from '../../hypermind/schema/approval';
import { Assertion, AssertionT } from '../../hypermind/schema/assertion';
import { Attestation, AttestationT } from '../../hypermind/schema/attestation';
import { Authority } from '../../hypermind/schema/authority';
import { Binding, BindingT } from '../../hypermind/schema/binding';
import { Checkpoint, CheckpointT } from '../../hypermind/schema/checkpoint';
import { Consolidation, ConsolidationT } from '../../hypermind/schema/consolidation';
import { DeliveredMsg, DeliveredMsgT } from '../../hypermind/schema/delivered-msg';
import { Effect, EffectT } from '../../hypermind/schema/effect';
import { Embedding, EmbeddingT } from '../../hypermind/schema/embedding';
import { EventPayload, unionToEventPayload, unionListToEventPayload } from '../../hypermind/schema/event-payload';
import { IntentSet, IntentSetT } from '../../hypermind/schema/intent-set';
import { LoopClosed, LoopClosedT } from '../../hypermind/schema/loop-closed';
import { LoopOpened, LoopOpenedT } from '../../hypermind/schema/loop-opened';
import { MediaRef, MediaRefT } from '../../hypermind/schema/media-ref';
import { ModelProvenance, ModelProvenanceT } from '../../hypermind/schema/model-provenance';
import { Outcome, OutcomeT } from '../../hypermind/schema/outcome';
import { ProposedAssertion, ProposedAssertionT } from '../../hypermind/schema/proposed-assertion';
import { ProviderFrame, ProviderFrameT } from '../../hypermind/schema/provider-frame';
import { Reasoning, ReasoningT } from '../../hypermind/schema/reasoning';
import { Recovery, RecoveryT } from '../../hypermind/schema/recovery';
import { Retention } from '../../hypermind/schema/retention';
import { Retract, RetractT } from '../../hypermind/schema/retract';
import { Sensitivity } from '../../hypermind/schema/sensitivity';
import { Supervisor, SupervisorT } from '../../hypermind/schema/supervisor';
import { ToolCall, ToolCallT } from '../../hypermind/schema/tool-call';
import { ToolResult, ToolResultT } from '../../hypermind/schema/tool-result';
import { UserMsg, UserMsgT } from '../../hypermind/schema/user-msg';


export class EventEnvelope {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):EventEnvelope {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsEventEnvelope(bb:flatbuffers.ByteBuffer, obj?:EventEnvelope):EventEnvelope {
  return (obj || new EventEnvelope()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsEventEnvelope(bb:flatbuffers.ByteBuffer, obj?:EventEnvelope):EventEnvelope {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new EventEnvelope()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static bufferHasIdentifier(bb:flatbuffers.ByteBuffer):boolean {
  return bb.__has_identifier('NCEV');
}

schemaVersion():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 1;
}

payloadType():EventPayload {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : EventPayload.NONE;
}

payload<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

connectionId(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

connectionIdLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

connectionIdArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

clientSeq():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

clientEventIndex():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

clientEventCount():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

originActor():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

runId(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

runIdLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

runIdArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

modelProvenance(obj?:ModelProvenance):ModelProvenance|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? (obj || new ModelProvenance()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

authority():Authority {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : Authority.user_asserted;
}

retention():Retention {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : Retention.current_state;
}

sensitivity():Sensitivity {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : Sensitivity.public_;
}

eventTimeNs():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

static startEventEnvelope(builder:flatbuffers.Builder) {
  builder.startObject(14);
}

static addSchemaVersion(builder:flatbuffers.Builder, schemaVersion:number) {
  builder.addFieldInt16(0, schemaVersion, 1);
}

static addPayloadType(builder:flatbuffers.Builder, payloadType:EventPayload) {
  builder.addFieldInt8(1, payloadType, EventPayload.NONE);
}

static addPayload(builder:flatbuffers.Builder, payloadOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, payloadOffset, 0);
}

static addConnectionId(builder:flatbuffers.Builder, connectionIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, connectionIdOffset, 0);
}

static createConnectionIdVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startConnectionIdVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addClientSeq(builder:flatbuffers.Builder, clientSeq:bigint) {
  builder.addFieldInt64(4, clientSeq, BigInt('0'));
}

static addClientEventIndex(builder:flatbuffers.Builder, clientEventIndex:number) {
  builder.addFieldInt32(5, clientEventIndex, 0);
}

static addClientEventCount(builder:flatbuffers.Builder, clientEventCount:number) {
  builder.addFieldInt32(6, clientEventCount, 0);
}

static addOriginActor(builder:flatbuffers.Builder, originActor:number) {
  builder.addFieldInt16(7, originActor, 0);
}

static addRunId(builder:flatbuffers.Builder, runIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, runIdOffset, 0);
}

static createRunIdVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startRunIdVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addModelProvenance(builder:flatbuffers.Builder, modelProvenanceOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, modelProvenanceOffset, 0);
}

static addAuthority(builder:flatbuffers.Builder, authority:Authority) {
  builder.addFieldInt8(10, authority, Authority.user_asserted);
}

static addRetention(builder:flatbuffers.Builder, retention:Retention) {
  builder.addFieldInt8(11, retention, Retention.current_state);
}

static addSensitivity(builder:flatbuffers.Builder, sensitivity:Sensitivity) {
  builder.addFieldInt8(12, sensitivity, Sensitivity.public_);
}

static addEventTimeNs(builder:flatbuffers.Builder, eventTimeNs:bigint) {
  builder.addFieldInt64(13, eventTimeNs, BigInt('0'));
}

static endEventEnvelope(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 8) // payload
  return offset;
}

static finishEventEnvelopeBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'NCEV');
}

static finishSizePrefixedEventEnvelopeBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, 'NCEV', true);
}


unpack(): EventEnvelopeT {
  return new EventEnvelopeT(
    this.schemaVersion(),
    this.payloadType(),
    (() => {
      let temp = unionToEventPayload(this.payloadType(), this.payload.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })(),
    this.bb!.createScalarList(this.connectionId.bind(this), this.connectionIdLength()),
    this.clientSeq(),
    this.clientEventIndex(),
    this.clientEventCount(),
    this.originActor(),
    this.bb!.createScalarList(this.runId.bind(this), this.runIdLength()),
    (this.modelProvenance() !== null ? this.modelProvenance()!.unpack() : null),
    this.authority(),
    this.retention(),
    this.sensitivity(),
    this.eventTimeNs()
  );
}


unpackTo(_o: EventEnvelopeT): void {
  _o.schemaVersion = this.schemaVersion();
  _o.payloadType = this.payloadType();
  _o.payload = (() => {
      let temp = unionToEventPayload(this.payloadType(), this.payload.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })();
  _o.connectionId = this.bb!.createScalarList(this.connectionId.bind(this), this.connectionIdLength());
  _o.clientSeq = this.clientSeq();
  _o.clientEventIndex = this.clientEventIndex();
  _o.clientEventCount = this.clientEventCount();
  _o.originActor = this.originActor();
  _o.runId = this.bb!.createScalarList(this.runId.bind(this), this.runIdLength());
  _o.modelProvenance = (this.modelProvenance() !== null ? this.modelProvenance()!.unpack() : null);
  _o.authority = this.authority();
  _o.retention = this.retention();
  _o.sensitivity = this.sensitivity();
  _o.eventTimeNs = this.eventTimeNs();
}
}

export class EventEnvelopeT {
constructor(
  public schemaVersion: number = 1,
  public payloadType: EventPayload = EventPayload.NONE,
  public payload: ApprovalT|AssertionT|AttestationT|BindingT|CheckpointT|ConsolidationT|DeliveredMsgT|EffectT|EmbeddingT|IntentSetT|LoopClosedT|LoopOpenedT|MediaRefT|OutcomeT|ProposedAssertionT|ProviderFrameT|ReasoningT|RecoveryT|RetractT|SupervisorT|ToolCallT|ToolResultT|UserMsgT|null = null,
  public connectionId: (number)[] = [],
  public clientSeq: bigint = BigInt('0'),
  public clientEventIndex: number = 0,
  public clientEventCount: number = 0,
  public originActor: number = 0,
  public runId: (number)[] = [],
  public modelProvenance: ModelProvenanceT|null = null,
  public authority: Authority = Authority.user_asserted,
  public retention: Retention = Retention.current_state,
  public sensitivity: Sensitivity = Sensitivity.public_,
  public eventTimeNs: bigint = BigInt('0')
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const payload = builder.createObjectOffset(this.payload);
  const connectionId = EventEnvelope.createConnectionIdVector(builder, this.connectionId);
  const runId = EventEnvelope.createRunIdVector(builder, this.runId);
  const modelProvenance = (this.modelProvenance !== null ? this.modelProvenance!.pack(builder) : 0);

  EventEnvelope.startEventEnvelope(builder);
  EventEnvelope.addSchemaVersion(builder, this.schemaVersion);
  EventEnvelope.addPayloadType(builder, this.payloadType);
  EventEnvelope.addPayload(builder, payload);
  EventEnvelope.addConnectionId(builder, connectionId);
  EventEnvelope.addClientSeq(builder, this.clientSeq);
  EventEnvelope.addClientEventIndex(builder, this.clientEventIndex);
  EventEnvelope.addClientEventCount(builder, this.clientEventCount);
  EventEnvelope.addOriginActor(builder, this.originActor);
  EventEnvelope.addRunId(builder, runId);
  EventEnvelope.addModelProvenance(builder, modelProvenance);
  EventEnvelope.addAuthority(builder, this.authority);
  EventEnvelope.addRetention(builder, this.retention);
  EventEnvelope.addSensitivity(builder, this.sensitivity);
  EventEnvelope.addEventTimeNs(builder, this.eventTimeNs);

  return EventEnvelope.endEventEnvelope(builder);
}
}
