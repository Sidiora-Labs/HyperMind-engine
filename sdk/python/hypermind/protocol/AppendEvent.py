# automatically generated by the FlatBuffers compiler, do not modify

# namespace: protocol

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class AppendEvent(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = AppendEvent()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAppendEvent(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def AppendEventBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x43\x50\x52", size_prefixed=size_prefixed)

    # AppendEvent
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # AppendEvent
    def Kind(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # AppendEvent
    def Conversation(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # AppendEvent
    def ConversationAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # AppendEvent
    def ConversationLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # AppendEvent
    def ConversationIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # AppendEvent
    def Payload(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # AppendEvent
    def PayloadAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # AppendEvent
    def PayloadLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # AppendEvent
    def PayloadIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

def AppendEventStart(builder): builder.StartObject(3)
def Start(builder):
    return AppendEventStart(builder)
def AppendEventAddKind(builder, kind): builder.PrependUint8Slot(0, kind, 0)
def AddKind(builder, kind):
    return AppendEventAddKind(builder, kind)
def AppendEventAddConversation(builder, conversation): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(conversation), 0)
def AddConversation(builder, conversation):
    return AppendEventAddConversation(builder, conversation)
def AppendEventStartConversationVector(builder, numElems): return builder.StartVector(1, numElems, 1)
def StartConversationVector(builder, numElems):
    return AppendEventStartConversationVector(builder, numElems)
def AppendEventAddPayload(builder, payload): builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(payload), 0)
def AddPayload(builder, payload):
    return AppendEventAddPayload(builder, payload)
def AppendEventStartPayloadVector(builder, numElems): return builder.StartVector(1, numElems, 1)
def StartPayloadVector(builder, numElems):
    return AppendEventStartPayloadVector(builder, numElems)
def AppendEventEnd(builder): return builder.EndObject()
def End(builder):
    return AppendEventEnd(builder)
try:
    from typing import List
except:
    pass

class AppendEventT(object):

    # AppendEventT
    def __init__(self):
        self.kind = 0  # type: int
        self.conversation = None  # type: List[int]
        self.payload = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        appendEvent = AppendEvent()
        appendEvent.Init(buf, pos)
        return cls.InitFromObj(appendEvent)

    @classmethod
    def InitFromObj(cls, appendEvent):
        x = AppendEventT()
        x._UnPack(appendEvent)
        return x

    # AppendEventT
    def _UnPack(self, appendEvent):
        if appendEvent is None:
            return
        self.kind = appendEvent.Kind()
        if not appendEvent.ConversationIsNone():
            if np is None:
                self.conversation = []
                for i in range(appendEvent.ConversationLength()):
                    self.conversation.append(appendEvent.Conversation(i))
            else:
                self.conversation = appendEvent.ConversationAsNumpy()
        if not appendEvent.PayloadIsNone():
            if np is None:
                self.payload = []
                for i in range(appendEvent.PayloadLength()):
                    self.payload.append(appendEvent.Payload(i))
            else:
                self.payload = appendEvent.PayloadAsNumpy()

    # AppendEventT
    def Pack(self, builder):
        if self.conversation is not None:
            if np is not None and type(self.conversation) is np.ndarray:
                conversation = builder.CreateNumpyVector(self.conversation)
            else:
                AppendEventStartConversationVector(builder, len(self.conversation))
                for i in reversed(range(len(self.conversation))):
                    builder.PrependUint8(self.conversation[i])
                conversation = builder.EndVector()
        if self.payload is not None:
            if np is not None and type(self.payload) is np.ndarray:
                payload = builder.CreateNumpyVector(self.payload)
            else:
                AppendEventStartPayloadVector(builder, len(self.payload))
                for i in reversed(range(len(self.payload))):
                    builder.PrependUint8(self.payload[i])
                payload = builder.EndVector()
        AppendEventStart(builder)
        AppendEventAddKind(builder, self.kind)
        if self.conversation is not None:
            AppendEventAddConversation(builder, conversation)
        if self.payload is not None:
            AppendEventAddPayload(builder, payload)
        appendEvent = AppendEventEnd(builder)
        return appendEvent
