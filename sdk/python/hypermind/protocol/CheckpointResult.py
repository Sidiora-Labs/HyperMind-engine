# automatically generated by the FlatBuffers compiler, do not modify

# namespace: protocol

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Returns the latest opaque turn state without interpreting its bytes.
class CheckpointResult(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CheckpointResult()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCheckpointResult(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CheckpointResultBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x43\x50\x52", size_prefixed=size_prefixed)

    # CheckpointResult
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CheckpointResult
    def Present(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # CheckpointResult
    def Lsn(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # CheckpointResult
    def Blob(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # CheckpointResult
    def BlobAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # CheckpointResult
    def BlobLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CheckpointResult
    def BlobIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

def CheckpointResultStart(builder): builder.StartObject(3)
def Start(builder):
    return CheckpointResultStart(builder)
def CheckpointResultAddPresent(builder, present): builder.PrependBoolSlot(0, present, 0)
def AddPresent(builder, present):
    return CheckpointResultAddPresent(builder, present)
def CheckpointResultAddLsn(builder, lsn): builder.PrependUint64Slot(1, lsn, 0)
def AddLsn(builder, lsn):
    return CheckpointResultAddLsn(builder, lsn)
def CheckpointResultAddBlob(builder, blob): builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(blob), 0)
def AddBlob(builder, blob):
    return CheckpointResultAddBlob(builder, blob)
def CheckpointResultStartBlobVector(builder, numElems): return builder.StartVector(1, numElems, 1)
def StartBlobVector(builder, numElems):
    return CheckpointResultStartBlobVector(builder, numElems)
def CheckpointResultEnd(builder): return builder.EndObject()
def End(builder):
    return CheckpointResultEnd(builder)
try:
    from typing import List
except:
    pass

class CheckpointResultT(object):

    # CheckpointResultT
    def __init__(self):
        self.present = False  # type: bool
        self.lsn = 0  # type: int
        self.blob = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        checkpointResult = CheckpointResult()
        checkpointResult.Init(buf, pos)
        return cls.InitFromObj(checkpointResult)

    @classmethod
    def InitFromObj(cls, checkpointResult):
        x = CheckpointResultT()
        x._UnPack(checkpointResult)
        return x

    # CheckpointResultT
    def _UnPack(self, checkpointResult):
        if checkpointResult is None:
            return
        self.present = checkpointResult.Present()
        self.lsn = checkpointResult.Lsn()
        if not checkpointResult.BlobIsNone():
            if np is None:
                self.blob = []
                for i in range(checkpointResult.BlobLength()):
                    self.blob.append(checkpointResult.Blob(i))
            else:
                self.blob = checkpointResult.BlobAsNumpy()

    # CheckpointResultT
    def Pack(self, builder):
        if self.blob is not None:
            if np is not None and type(self.blob) is np.ndarray:
                blob = builder.CreateNumpyVector(self.blob)
            else:
                CheckpointResultStartBlobVector(builder, len(self.blob))
                for i in reversed(range(len(self.blob))):
                    builder.PrependUint8(self.blob[i])
                blob = builder.EndVector()
        CheckpointResultStart(builder)
        CheckpointResultAddPresent(builder, self.present)
        CheckpointResultAddLsn(builder, self.lsn)
        if self.blob is not None:
            CheckpointResultAddBlob(builder, blob)
        checkpointResult = CheckpointResultEnd(builder)
        return checkpointResult
