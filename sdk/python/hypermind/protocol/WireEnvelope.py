# automatically generated by the FlatBuffers compiler, do not modify

# namespace: protocol

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class WireEnvelope(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = WireEnvelope()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsWireEnvelope(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def WireEnvelopeBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x43\x50\x52", size_prefixed=size_prefixed)

    # WireEnvelope
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # WireEnvelope
    def ProtoVersion(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # WireEnvelope
    def PayloadType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # WireEnvelope
    def Payload(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            from flatbuffers.table import Table
            obj = Table(bytearray(), 0)
            self._tab.Union(obj, o)
            return obj
        return None

def WireEnvelopeStart(builder): builder.StartObject(3)
def Start(builder):
    return WireEnvelopeStart(builder)
def WireEnvelopeAddProtoVersion(builder, protoVersion): builder.PrependUint16Slot(0, protoVersion, 0)
def AddProtoVersion(builder, protoVersion):
    return WireEnvelopeAddProtoVersion(builder, protoVersion)
def WireEnvelopeAddPayloadType(builder, payloadType): builder.PrependUint8Slot(1, payloadType, 0)
def AddPayloadType(builder, payloadType):
    return WireEnvelopeAddPayloadType(builder, payloadType)
def WireEnvelopeAddPayload(builder, payload): builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(payload), 0)
def AddPayload(builder, payload):
    return WireEnvelopeAddPayload(builder, payload)
def WireEnvelopeEnd(builder): return builder.EndObject()
def End(builder):
    return WireEnvelopeEnd(builder)
import hypermind.protocol.Event
import hypermind.protocol.Hello
import hypermind.protocol.Request
import hypermind.protocol.Response
import hypermind.protocol.Welcome
import hypermind.protocol.WirePayload
try:
    from typing import Union
except:
    pass

class WireEnvelopeT(object):

    # WireEnvelopeT
    def __init__(self):
        self.protoVersion = 0  # type: int
        self.payloadType = 0  # type: int
        self.payload = None  # type: Union[None, hypermind.protocol.Hello.HelloT, hypermind.protocol.Welcome.WelcomeT, hypermind.protocol.Request.RequestT, hypermind.protocol.Response.ResponseT, hypermind.protocol.Event.EventT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        wireEnvelope = WireEnvelope()
        wireEnvelope.Init(buf, pos)
        return cls.InitFromObj(wireEnvelope)

    @classmethod
    def InitFromObj(cls, wireEnvelope):
        x = WireEnvelopeT()
        x._UnPack(wireEnvelope)
        return x

    # WireEnvelopeT
    def _UnPack(self, wireEnvelope):
        if wireEnvelope is None:
            return
        self.protoVersion = wireEnvelope.ProtoVersion()
        self.payloadType = wireEnvelope.PayloadType()
        self.payload = hypermind.protocol.WirePayload.WirePayloadCreator(self.payloadType, wireEnvelope.Payload())

    # WireEnvelopeT
    def Pack(self, builder):
        if self.payload is not None:
            payload = self.payload.Pack(builder)
        WireEnvelopeStart(builder)
        WireEnvelopeAddProtoVersion(builder, self.protoVersion)
        WireEnvelopeAddPayloadType(builder, self.payloadType)
        if self.payload is not None:
            WireEnvelopeAddPayload(builder, payload)
        wireEnvelope = WireEnvelopeEnd(builder)
        return wireEnvelope
