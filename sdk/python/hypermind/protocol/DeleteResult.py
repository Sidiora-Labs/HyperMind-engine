# automatically generated by the FlatBuffers compiler, do not modify

# namespace: protocol

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class DeleteResult(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = DeleteResult()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsDeleteResult(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def DeleteResultBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x43\x50\x52", size_prefixed=size_prefixed)

    # DeleteResult
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # DeleteResult
    def Actor(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return 0

    # DeleteResult
    def Receipt(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # DeleteResult
    def ReceiptAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # DeleteResult
    def ReceiptLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # DeleteResult
    def ReceiptIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def DeleteResultStart(builder): builder.StartObject(2)
def Start(builder):
    return DeleteResultStart(builder)
def DeleteResultAddActor(builder, actor): builder.PrependUint16Slot(0, actor, 0)
def AddActor(builder, actor):
    return DeleteResultAddActor(builder, actor)
def DeleteResultAddReceipt(builder, receipt): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(receipt), 0)
def AddReceipt(builder, receipt):
    return DeleteResultAddReceipt(builder, receipt)
def DeleteResultStartReceiptVector(builder, numElems): return builder.StartVector(1, numElems, 1)
def StartReceiptVector(builder, numElems):
    return DeleteResultStartReceiptVector(builder, numElems)
def DeleteResultEnd(builder): return builder.EndObject()
def End(builder):
    return DeleteResultEnd(builder)
try:
    from typing import List
except:
    pass

class DeleteResultT(object):

    # DeleteResultT
    def __init__(self):
        self.actor = 0  # type: int
        self.receipt = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        deleteResult = DeleteResult()
        deleteResult.Init(buf, pos)
        return cls.InitFromObj(deleteResult)

    @classmethod
    def InitFromObj(cls, deleteResult):
        x = DeleteResultT()
        x._UnPack(deleteResult)
        return x

    # DeleteResultT
    def _UnPack(self, deleteResult):
        if deleteResult is None:
            return
        self.actor = deleteResult.Actor()
        if not deleteResult.ReceiptIsNone():
            if np is None:
                self.receipt = []
                for i in range(deleteResult.ReceiptLength()):
                    self.receipt.append(deleteResult.Receipt(i))
            else:
                self.receipt = deleteResult.ReceiptAsNumpy()

    # DeleteResultT
    def Pack(self, builder):
        if self.receipt is not None:
            if np is not None and type(self.receipt) is np.ndarray:
                receipt = builder.CreateNumpyVector(self.receipt)
            else:
                DeleteResultStartReceiptVector(builder, len(self.receipt))
                for i in reversed(range(len(self.receipt))):
                    builder.PrependUint8(self.receipt[i])
                receipt = builder.EndVector()
        DeleteResultStart(builder)
        DeleteResultAddActor(builder, self.actor)
        if self.receipt is not None:
            DeleteResultAddReceipt(builder, receipt)
        deleteResult = DeleteResultEnd(builder)
        return deleteResult
