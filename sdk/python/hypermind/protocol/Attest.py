# automatically generated by the FlatBuffers compiler, do not modify

# namespace: protocol

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class Attest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Attest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAttest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def AttestBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x43\x50\x52", size_prefixed=size_prefixed)

    # Attest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Attest
    def Used(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # Attest
    def UsedAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint64Flags, o)
        return 0

    # Attest
    def UsedLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Attest
    def UsedIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Attest
    def Ignored(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # Attest
    def IgnoredAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint64Flags, o)
        return 0

    # Attest
    def IgnoredLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Attest
    def IgnoredIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Attest
    def ClientSeq(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Attest
    def Helpful(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # Attest
    def HelpfulAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint64Flags, o)
        return 0

    # Attest
    def HelpfulLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Attest
    def HelpfulIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # Attest
    def Harmful(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # Attest
    def HarmfulAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint64Flags, o)
        return 0

    # Attest
    def HarmfulLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Attest
    def HarmfulIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def AttestStart(builder): builder.StartObject(5)
def Start(builder):
    return AttestStart(builder)
def AttestAddUsed(builder, used): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(used), 0)
def AddUsed(builder, used):
    return AttestAddUsed(builder, used)
def AttestStartUsedVector(builder, numElems): return builder.StartVector(8, numElems, 8)
def StartUsedVector(builder, numElems):
    return AttestStartUsedVector(builder, numElems)
def AttestAddIgnored(builder, ignored): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(ignored), 0)
def AddIgnored(builder, ignored):
    return AttestAddIgnored(builder, ignored)
def AttestStartIgnoredVector(builder, numElems): return builder.StartVector(8, numElems, 8)
def StartIgnoredVector(builder, numElems):
    return AttestStartIgnoredVector(builder, numElems)
def AttestAddClientSeq(builder, clientSeq): builder.PrependUint64Slot(2, clientSeq, 0)
def AddClientSeq(builder, clientSeq):
    return AttestAddClientSeq(builder, clientSeq)
def AttestAddHelpful(builder, helpful): builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(helpful), 0)
def AddHelpful(builder, helpful):
    return AttestAddHelpful(builder, helpful)
def AttestStartHelpfulVector(builder, numElems): return builder.StartVector(8, numElems, 8)
def StartHelpfulVector(builder, numElems):
    return AttestStartHelpfulVector(builder, numElems)
def AttestAddHarmful(builder, harmful): builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(harmful), 0)
def AddHarmful(builder, harmful):
    return AttestAddHarmful(builder, harmful)
def AttestStartHarmfulVector(builder, numElems): return builder.StartVector(8, numElems, 8)
def StartHarmfulVector(builder, numElems):
    return AttestStartHarmfulVector(builder, numElems)
def AttestEnd(builder): return builder.EndObject()
def End(builder):
    return AttestEnd(builder)
try:
    from typing import List
except:
    pass

class AttestT(object):

    # AttestT
    def __init__(self):
        self.used = None  # type: List[int]
        self.ignored = None  # type: List[int]
        self.clientSeq = 0  # type: int
        self.helpful = None  # type: List[int]
        self.harmful = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        attest = Attest()
        attest.Init(buf, pos)
        return cls.InitFromObj(attest)

    @classmethod
    def InitFromObj(cls, attest):
        x = AttestT()
        x._UnPack(attest)
        return x

    # AttestT
    def _UnPack(self, attest):
        if attest is None:
            return
        if not attest.UsedIsNone():
            if np is None:
                self.used = []
                for i in range(attest.UsedLength()):
                    self.used.append(attest.Used(i))
            else:
                self.used = attest.UsedAsNumpy()
        if not attest.IgnoredIsNone():
            if np is None:
                self.ignored = []
                for i in range(attest.IgnoredLength()):
                    self.ignored.append(attest.Ignored(i))
            else:
                self.ignored = attest.IgnoredAsNumpy()
        self.clientSeq = attest.ClientSeq()
        if not attest.HelpfulIsNone():
            if np is None:
                self.helpful = []
                for i in range(attest.HelpfulLength()):
                    self.helpful.append(attest.Helpful(i))
            else:
                self.helpful = attest.HelpfulAsNumpy()
        if not attest.HarmfulIsNone():
            if np is None:
                self.harmful = []
                for i in range(attest.HarmfulLength()):
                    self.harmful.append(attest.Harmful(i))
            else:
                self.harmful = attest.HarmfulAsNumpy()

    # AttestT
    def Pack(self, builder):
        if self.used is not None:
            if np is not None and type(self.used) is np.ndarray:
                used = builder.CreateNumpyVector(self.used)
            else:
                AttestStartUsedVector(builder, len(self.used))
                for i in reversed(range(len(self.used))):
                    builder.PrependUint64(self.used[i])
                used = builder.EndVector()
        if self.ignored is not None:
            if np is not None and type(self.ignored) is np.ndarray:
                ignored = builder.CreateNumpyVector(self.ignored)
            else:
                AttestStartIgnoredVector(builder, len(self.ignored))
                for i in reversed(range(len(self.ignored))):
                    builder.PrependUint64(self.ignored[i])
                ignored = builder.EndVector()
        if self.helpful is not None:
            if np is not None and type(self.helpful) is np.ndarray:
                helpful = builder.CreateNumpyVector(self.helpful)
            else:
                AttestStartHelpfulVector(builder, len(self.helpful))
                for i in reversed(range(len(self.helpful))):
                    builder.PrependUint64(self.helpful[i])
                helpful = builder.EndVector()
        if self.harmful is not None:
            if np is not None and type(self.harmful) is np.ndarray:
                harmful = builder.CreateNumpyVector(self.harmful)
            else:
                AttestStartHarmfulVector(builder, len(self.harmful))
                for i in reversed(range(len(self.harmful))):
                    builder.PrependUint64(self.harmful[i])
                harmful = builder.EndVector()
        AttestStart(builder)
        if self.used is not None:
            AttestAddUsed(builder, used)
        if self.ignored is not None:
            AttestAddIgnored(builder, ignored)
        AttestAddClientSeq(builder, self.clientSeq)
        if self.helpful is not None:
            AttestAddHelpful(builder, helpful)
        if self.harmful is not None:
            AttestAddHarmful(builder, harmful)
        attest = AttestEnd(builder)
        return attest
