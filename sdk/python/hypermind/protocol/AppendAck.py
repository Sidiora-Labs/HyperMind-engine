# automatically generated by the FlatBuffers compiler, do not modify

# namespace: protocol

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class AppendAck(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = AppendAck()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAppendAck(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def AppendAckBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x43\x50\x52", size_prefixed=size_prefixed)

    # AppendAck
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # AppendAck
    def ClientSeq(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # AppendAck
    def FirstLsn(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # AppendAck
    def LastLsn(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # AppendAck
    def Duplicate(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # AppendAck
    def LeafCount(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # AppendAck
    def LastLeafHash(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # AppendAck
    def LastLeafHashAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # AppendAck
    def LastLeafHashLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # AppendAck
    def LastLeafHashIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # AppendAck
    def MmrRoot(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # AppendAck
    def MmrRootAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # AppendAck
    def MmrRootLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # AppendAck
    def MmrRootIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

def AppendAckStart(builder): builder.StartObject(7)
def Start(builder):
    return AppendAckStart(builder)
def AppendAckAddClientSeq(builder, clientSeq): builder.PrependUint64Slot(0, clientSeq, 0)
def AddClientSeq(builder, clientSeq):
    return AppendAckAddClientSeq(builder, clientSeq)
def AppendAckAddFirstLsn(builder, firstLsn): builder.PrependUint64Slot(1, firstLsn, 0)
def AddFirstLsn(builder, firstLsn):
    return AppendAckAddFirstLsn(builder, firstLsn)
def AppendAckAddLastLsn(builder, lastLsn): builder.PrependUint64Slot(2, lastLsn, 0)
def AddLastLsn(builder, lastLsn):
    return AppendAckAddLastLsn(builder, lastLsn)
def AppendAckAddDuplicate(builder, duplicate): builder.PrependBoolSlot(3, duplicate, 0)
def AddDuplicate(builder, duplicate):
    return AppendAckAddDuplicate(builder, duplicate)
def AppendAckAddLeafCount(builder, leafCount): builder.PrependUint64Slot(4, leafCount, 0)
def AddLeafCount(builder, leafCount):
    return AppendAckAddLeafCount(builder, leafCount)
def AppendAckAddLastLeafHash(builder, lastLeafHash): builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(lastLeafHash), 0)
def AddLastLeafHash(builder, lastLeafHash):
    return AppendAckAddLastLeafHash(builder, lastLeafHash)
def AppendAckStartLastLeafHashVector(builder, numElems): return builder.StartVector(1, numElems, 1)
def StartLastLeafHashVector(builder, numElems):
    return AppendAckStartLastLeafHashVector(builder, numElems)
def AppendAckAddMmrRoot(builder, mmrRoot): builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(mmrRoot), 0)
def AddMmrRoot(builder, mmrRoot):
    return AppendAckAddMmrRoot(builder, mmrRoot)
def AppendAckStartMmrRootVector(builder, numElems): return builder.StartVector(1, numElems, 1)
def StartMmrRootVector(builder, numElems):
    return AppendAckStartMmrRootVector(builder, numElems)
def AppendAckEnd(builder): return builder.EndObject()
def End(builder):
    return AppendAckEnd(builder)
try:
    from typing import List
except:
    pass

class AppendAckT(object):

    # AppendAckT
    def __init__(self):
        self.clientSeq = 0  # type: int
        self.firstLsn = 0  # type: int
        self.lastLsn = 0  # type: int
        self.duplicate = False  # type: bool
        self.leafCount = 0  # type: int
        self.lastLeafHash = None  # type: List[int]
        self.mmrRoot = None  # type: List[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        appendAck = AppendAck()
        appendAck.Init(buf, pos)
        return cls.InitFromObj(appendAck)

    @classmethod
    def InitFromObj(cls, appendAck):
        x = AppendAckT()
        x._UnPack(appendAck)
        return x

    # AppendAckT
    def _UnPack(self, appendAck):
        if appendAck is None:
            return
        self.clientSeq = appendAck.ClientSeq()
        self.firstLsn = appendAck.FirstLsn()
        self.lastLsn = appendAck.LastLsn()
        self.duplicate = appendAck.Duplicate()
        self.leafCount = appendAck.LeafCount()
        if not appendAck.LastLeafHashIsNone():
            if np is None:
                self.lastLeafHash = []
                for i in range(appendAck.LastLeafHashLength()):
                    self.lastLeafHash.append(appendAck.LastLeafHash(i))
            else:
                self.lastLeafHash = appendAck.LastLeafHashAsNumpy()
        if not appendAck.MmrRootIsNone():
            if np is None:
                self.mmrRoot = []
                for i in range(appendAck.MmrRootLength()):
                    self.mmrRoot.append(appendAck.MmrRoot(i))
            else:
                self.mmrRoot = appendAck.MmrRootAsNumpy()

    # AppendAckT
    def Pack(self, builder):
        if self.lastLeafHash is not None:
            if np is not None and type(self.lastLeafHash) is np.ndarray:
                lastLeafHash = builder.CreateNumpyVector(self.lastLeafHash)
            else:
                AppendAckStartLastLeafHashVector(builder, len(self.lastLeafHash))
                for i in reversed(range(len(self.lastLeafHash))):
                    builder.PrependUint8(self.lastLeafHash[i])
                lastLeafHash = builder.EndVector()
        if self.mmrRoot is not None:
            if np is not None and type(self.mmrRoot) is np.ndarray:
                mmrRoot = builder.CreateNumpyVector(self.mmrRoot)
            else:
                AppendAckStartMmrRootVector(builder, len(self.mmrRoot))
                for i in reversed(range(len(self.mmrRoot))):
                    builder.PrependUint8(self.mmrRoot[i])
                mmrRoot = builder.EndVector()
        AppendAckStart(builder)
        AppendAckAddClientSeq(builder, self.clientSeq)
        AppendAckAddFirstLsn(builder, self.firstLsn)
        AppendAckAddLastLsn(builder, self.lastLsn)
        AppendAckAddDuplicate(builder, self.duplicate)
        AppendAckAddLeafCount(builder, self.leafCount)
        if self.lastLeafHash is not None:
            AppendAckAddLastLeafHash(builder, lastLeafHash)
        if self.mmrRoot is not None:
            AppendAckAddMmrRoot(builder, mmrRoot)
        appendAck = AppendAckEnd(builder)
        return appendAck
