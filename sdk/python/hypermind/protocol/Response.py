# automatically generated by the FlatBuffers compiler, do not modify

# namespace: protocol

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class Response(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Response()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsResponse(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def ResponseBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x43\x50\x52", size_prefixed=size_prefixed)

    # Response
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Response
    def RequestId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Response
    def Status(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Response
    def PayloadType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Response
    def Payload(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            from flatbuffers.table import Table
            obj = Table(bytearray(), 0)
            self._tab.Union(obj, o)
            return obj
        return None

def ResponseStart(builder): builder.StartObject(4)
def Start(builder):
    return ResponseStart(builder)
def ResponseAddRequestId(builder, requestId): builder.PrependUint64Slot(0, requestId, 0)
def AddRequestId(builder, requestId):
    return ResponseAddRequestId(builder, requestId)
def ResponseAddStatus(builder, status): builder.PrependUint8Slot(1, status, 0)
def AddStatus(builder, status):
    return ResponseAddStatus(builder, status)
def ResponseAddPayloadType(builder, payloadType): builder.PrependUint8Slot(2, payloadType, 0)
def AddPayloadType(builder, payloadType):
    return ResponseAddPayloadType(builder, payloadType)
def ResponseAddPayload(builder, payload): builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(payload), 0)
def AddPayload(builder, payload):
    return ResponseAddPayload(builder, payload)
def ResponseEnd(builder): return builder.EndObject()
def End(builder):
    return ResponseEnd(builder)
import hypermind.protocol.AppendAck
import hypermind.protocol.AttestAck
import hypermind.protocol.BeliefResult
import hypermind.protocol.BytesResult
import hypermind.protocol.CheckpointAck
import hypermind.protocol.CheckpointResult
import hypermind.protocol.DeleteResult
import hypermind.protocol.ErrorDetail
import hypermind.protocol.HealthResult
import hypermind.protocol.LatencyResult
import hypermind.protocol.RebuildResult
import hypermind.protocol.RecallResult
import hypermind.protocol.ResponsePayload
import hypermind.protocol.StatsResult
import hypermind.protocol.SubscriptionAck
import hypermind.protocol.TranscriptResult
import hypermind.protocol.VerifyResult
try:
    from typing import Union
except:
    pass

class ResponseT(object):

    # ResponseT
    def __init__(self):
        self.requestId = 0  # type: int
        self.status = 0  # type: int
        self.payloadType = 0  # type: int
        self.payload = None  # type: Union[None, hypermind.protocol.ErrorDetail.ErrorDetailT, hypermind.protocol.AppendAck.AppendAckT, hypermind.protocol.BytesResult.BytesResultT, hypermind.protocol.SubscriptionAck.SubscriptionAckT, hypermind.protocol.HealthResult.HealthResultT, hypermind.protocol.StatsResult.StatsResultT, hypermind.protocol.LatencyResult.LatencyResultT, hypermind.protocol.VerifyResult.VerifyResultT, hypermind.protocol.RebuildResult.RebuildResultT, hypermind.protocol.DeleteResult.DeleteResultT, hypermind.protocol.TranscriptResult.TranscriptResultT, hypermind.protocol.RecallResult.RecallResultT, hypermind.protocol.BeliefResult.BeliefResultT, hypermind.protocol.CheckpointAck.CheckpointAckT, hypermind.protocol.CheckpointResult.CheckpointResultT, hypermind.protocol.AttestAck.AttestAckT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        response = Response()
        response.Init(buf, pos)
        return cls.InitFromObj(response)

    @classmethod
    def InitFromObj(cls, response):
        x = ResponseT()
        x._UnPack(response)
        return x

    # ResponseT
    def _UnPack(self, response):
        if response is None:
            return
        self.requestId = response.RequestId()
        self.status = response.Status()
        self.payloadType = response.PayloadType()
        self.payload = hypermind.protocol.ResponsePayload.ResponsePayloadCreator(self.payloadType, response.Payload())

    # ResponseT
    def Pack(self, builder):
        if self.payload is not None:
            payload = self.payload.Pack(builder)
        ResponseStart(builder)
        ResponseAddRequestId(builder, self.requestId)
        ResponseAddStatus(builder, self.status)
        ResponseAddPayloadType(builder, self.payloadType)
        if self.payload is not None:
            ResponseAddPayload(builder, payload)
        response = ResponseEnd(builder)
        return response
