# automatically generated by the FlatBuffers compiler, do not modify

# namespace: protocol

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class TranscriptResult(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TranscriptResult()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTranscriptResult(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def TranscriptResultBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x43\x50\x52", size_prefixed=size_prefixed)

    # TranscriptResult
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TranscriptResult
    def Records(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from hypermind.protocol.FrameRecord import FrameRecord
            obj = FrameRecord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TranscriptResult
    def RecordsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TranscriptResult
    def RecordsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # TranscriptResult
    def Truncated(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def TranscriptResultStart(builder): builder.StartObject(2)
def Start(builder):
    return TranscriptResultStart(builder)
def TranscriptResultAddRecords(builder, records): builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(records), 0)
def AddRecords(builder, records):
    return TranscriptResultAddRecords(builder, records)
def TranscriptResultStartRecordsVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartRecordsVector(builder, numElems):
    return TranscriptResultStartRecordsVector(builder, numElems)
def TranscriptResultAddTruncated(builder, truncated): builder.PrependBoolSlot(1, truncated, 0)
def AddTruncated(builder, truncated):
    return TranscriptResultAddTruncated(builder, truncated)
def TranscriptResultEnd(builder): return builder.EndObject()
def End(builder):
    return TranscriptResultEnd(builder)
import hypermind.protocol.FrameRecord
try:
    from typing import List
except:
    pass

class TranscriptResultT(object):

    # TranscriptResultT
    def __init__(self):
        self.records = None  # type: List[hypermind.protocol.FrameRecord.FrameRecordT]
        self.truncated = False  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        transcriptResult = TranscriptResult()
        transcriptResult.Init(buf, pos)
        return cls.InitFromObj(transcriptResult)

    @classmethod
    def InitFromObj(cls, transcriptResult):
        x = TranscriptResultT()
        x._UnPack(transcriptResult)
        return x

    # TranscriptResultT
    def _UnPack(self, transcriptResult):
        if transcriptResult is None:
            return
        if not transcriptResult.RecordsIsNone():
            self.records = []
            for i in range(transcriptResult.RecordsLength()):
                if transcriptResult.Records(i) is None:
                    self.records.append(None)
                else:
                    frameRecord_ = hypermind.protocol.FrameRecord.FrameRecordT.InitFromObj(transcriptResult.Records(i))
                    self.records.append(frameRecord_)
        self.truncated = transcriptResult.Truncated()

    # TranscriptResultT
    def Pack(self, builder):
        if self.records is not None:
            recordslist = []
            for i in range(len(self.records)):
                recordslist.append(self.records[i].Pack(builder))
            TranscriptResultStartRecordsVector(builder, len(self.records))
            for i in reversed(range(len(self.records))):
                builder.PrependUOffsetTRelative(recordslist[i])
            records = builder.EndVector()
        TranscriptResultStart(builder)
        if self.records is not None:
            TranscriptResultAddRecords(builder, records)
        TranscriptResultAddTruncated(builder, self.truncated)
        transcriptResult = TranscriptResultEnd(builder)
        return transcriptResult
