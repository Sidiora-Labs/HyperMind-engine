# automatically generated by the FlatBuffers compiler, do not modify

# namespace: protocol

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class Append(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Append()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAppend(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def AppendBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x4E\x43\x50\x52", size_prefixed=size_prefixed)

    # Append
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Append
    def ClientSeq(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # Append
    def Events(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from hypermind.protocol.AppendEvent import AppendEvent
            obj = AppendEvent()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Append
    def EventsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Append
    def EventsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def AppendStart(builder): builder.StartObject(2)
def Start(builder):
    return AppendStart(builder)
def AppendAddClientSeq(builder, clientSeq): builder.PrependUint64Slot(0, clientSeq, 0)
def AddClientSeq(builder, clientSeq):
    return AppendAddClientSeq(builder, clientSeq)
def AppendAddEvents(builder, events): builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(events), 0)
def AddEvents(builder, events):
    return AppendAddEvents(builder, events)
def AppendStartEventsVector(builder, numElems): return builder.StartVector(4, numElems, 4)
def StartEventsVector(builder, numElems):
    return AppendStartEventsVector(builder, numElems)
def AppendEnd(builder): return builder.EndObject()
def End(builder):
    return AppendEnd(builder)
import hypermind.protocol.AppendEvent
try:
    from typing import List
except:
    pass

class AppendT(object):

    # AppendT
    def __init__(self):
        self.clientSeq = 0  # type: int
        self.events = None  # type: List[hypermind.protocol.AppendEvent.AppendEventT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        append = Append()
        append.Init(buf, pos)
        return cls.InitFromObj(append)

    @classmethod
    def InitFromObj(cls, append):
        x = AppendT()
        x._UnPack(append)
        return x

    # AppendT
    def _UnPack(self, append):
        if append is None:
            return
        self.clientSeq = append.ClientSeq()
        if not append.EventsIsNone():
            self.events = []
            for i in range(append.EventsLength()):
                if append.Events(i) is None:
                    self.events.append(None)
                else:
                    appendEvent_ = hypermind.protocol.AppendEvent.AppendEventT.InitFromObj(append.Events(i))
                    self.events.append(appendEvent_)

    # AppendT
    def Pack(self, builder):
        if self.events is not None:
            eventslist = []
            for i in range(len(self.events)):
                eventslist.append(self.events[i].Pack(builder))
            AppendStartEventsVector(builder, len(self.events))
            for i in reversed(range(len(self.events))):
                builder.PrependUOffsetTRelative(eventslist[i])
            events = builder.EndVector()
        AppendStart(builder)
        AppendAddClientSeq(builder, self.clientSeq)
        if self.events is not None:
            AppendAddEvents(builder, events)
        append = AppendEnd(builder)
        return append
